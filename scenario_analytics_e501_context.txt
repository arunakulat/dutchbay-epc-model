================================================================================
analytics/scenario_analytics.py - E501/B950 violations
================================================================================

Total violation sites (line numbers): 5


================================================================================
LINE 67
================================================================================
      64: 
      65: class ScenarioAnalytics:
      66:     """
>>>   67:     V14-style orchestrator for batch scenario analytics with full Go With The Flow features.
      68:     """
      69: 
      70:     def __init__(

    Length: 92 characters (limit: 88)
    Excess: 4 characters

================================================================================
LINE 90
================================================================================
      87:     # Scenario discovery and filters
      88:     # ------------------------------------------------------------------
      89:     def discover_scenarios(self) -> List[Path]:
>>>   90:         """Return a sorted list of scenario config paths under scenarios_dir, optionally filtered."""
      91:         if not self.scenarios_dir.exists():
      92:             raise FileNotFoundError(
      93:                 f"Scenarios directory not found: {self.scenarios_dir}"

    Length: 101 characters (limit: 88)
    Excess: 13 characters

================================================================================
LINE 120
================================================================================
     117:     # Discount rate logic
     118:     # ------------------------------------------------------------------
     119:     def _effective_discount_rate(self, config: Dict[str, Any]) -> float:
>>>  120:         """Extract discount rate per scenario using precedence: scenario > wacc > global default."""
     121:         # 1. Scenario override
     122:         scenario = config.get("scenario", {})  # legacy field
     123:         scenario_overrides = scenario.get("override", {}) if scenario else {}

    Length: 100 characters (limit: 88)
    Excess: 12 characters

================================================================================
LINE 230
================================================================================
     227: 
     228:         batch_run = self.parallel and len(scenario_paths) > 4
     229:         logger.info(
>>>  230:             f"Running {len(scenario_paths)} scenario(s) {'in parallel' if batch_run else 'serially'}."
     231:         )
     232: 
     233:         if batch_run:

    Length: 102 characters (limit: 88)
    Excess: 14 characters

================================================================================
LINE 368
================================================================================
     365:                 timeseries_df["dscr"] = timeseries_df[cfads_col] / denom
     366:             else:
     367:                 logger.warning(
>>>  368:                     f"Could not derive DSCR column: cfads_col={cfads_col}, debt_candidates={debt_candidates}"
     369:                 )
     370: 
     371:         summary_df, timeseries_df = normalise_kpis_for_export(

    Length: 109 characters (limit: 88)
    Excess: 21 characters