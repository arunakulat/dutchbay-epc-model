defaults:
  - override hydra/job_logging: disabled
  - override hydra/hydra_logging: disabled
  - _self_

mode: verify_retained_sources

hydra:
  run:
    dir: .
  job:
    chdir: false
  output_subdir: null
