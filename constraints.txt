# ─────────────────────────────────────────────────────────────────────────────
# FREEZE-REFRESH POLICY CONSTRAINTS (#756 post-mortem, 2026-07-04).
#
# Applied by `make lock` when regenerating requirements.txt in a clean Python
# 3.12 environment (see the lock header and Makefile for the exact recipe).
#
# These hold the versions the mandatory gates were CLEARED against. Lifting any
# line is a deliberate migration dolphin, not a dependency chore — mirrored by
# the semver-major ignore rules in .github/dependabot.yml.
# ─────────────────────────────────────────────────────────────────────────────
# pandas 3.0: mandatory Copy-on-Write + default str-dtype inference — unconditional
# breaking changes for the finance engine. Lift only via the KPI-oracle-verified
# migration tracked as #593 (also capped in pyproject).
pandas<3
# mypy: the lint gate pins the version the typed surface was cleared against
# (diagnostics change on minors); keep the freeze in lockstep with the workflow pin.
mypy==1.19.0
# isort 8 / ruff 0.15: mandatory-gate rule churn; bump in a dedicated lint dolphin.
isort<8
ruff<0.15
# black is CalVer — every yearly bump is a "major"; hold within the cleared year.
black<27
# Typed-surface toolchain held at the gate-cleared releases (newer stub/typing
# releases change mypy diagnostics; bump them in a dedicated typed-surface dolphin
# together with the accompanying type fixes):
pandas-stubs==2.3.3.251201
requests==2.33.0
# Document-ingestion output and hook behaviour are reproducibility surfaces. Hold
# the versions migrated from the retired 3.11 environment until a dedicated upgrade
# dolphin revalidates conversion, extraction, rendering, hooks, audit, and the suite.
markitdown==0.1.6
pdfplumber==0.11.10
pymupdf==1.28.0
pre-commit==4.6.2
# antlr4-python3-runtime: hard-capped by the Hydra stack, NOT a gate-cleared freeze.
# hydra-core 1.3.4 and omegaconf 2.3.1 both require antlr4-python3-runtime==4.9.* (the
# grammar runtime their config parsers were generated against), so 4.10+ is an
# unconditional ResolutionImpossible against those pins (#939 post-mortem: the weekly
# group bumped it to 4.13.2 and made the lock uninstallable). Lift only in lockstep
# with a hydra-core/omegaconf upgrade that regenerates their grammars.
antlr4-python3-runtime<4.10
