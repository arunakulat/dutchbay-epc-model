# Pre-commit hooks configuration (Go-With-The-Flow CI)

repos:
  - repo: https://github.com/psf/black
    rev: "25.11.0"
    hooks:
      - id: black
        language_version: python3.11
        args: ["--line-length=88"]
        exclude: ^legacy/

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: "v0.8.3"
    hooks:
      - id: ruff
        # Ruff will read configuration from ruff.toml
        args: ["--fix"]
        # Let ruff.toml handle which files/rules we care about.

  - repo: https://github.com/pycqa/isort
    rev: "7.0.0"
    hooks:
      - id: isort
        args: ["--profile=black"]
        exclude: ^legacy/

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: "v5.0.0"
    hooks:
      - id: check-added-large-files
      - id: check-ast
      - id: check-toml
      - id: check-yaml
      - id: end-of-file-fixer
        exclude: ^legacy/
      - id: trailing-whitespace
        exclude: ^legacy/
      - id: check-merge-conflict
      - id: debug-statements
        exclude: ^legacy/

  # Mypy disabled for Sprint Day N – runs in CI instead.
  # Re-enable when you want mypy on every commit:
  # - repo: https://github.com/pre-commit/mirrors-mypy
  #   rev: "v1.18.2"
  #   hooks:
  #     - id: mypy
  #       args:
  #         - "--config-file=mypy.ini"
  #       additional_dependencies:
  #         - pydantic
  #         - pydantic-settings
  #         - pydantic[mypy]
  #         - types-PyYAML
  #         - pandas-stubs
  #         - types-openpyxl
  #         - numpy-financial
  #         - scipy-stubs

# EOF
