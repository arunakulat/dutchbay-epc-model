"""Operational-introspection helpers.

Small, dependency-light utilities that let a RUNNING instance report its own state, so an
operator can verify a deployment without shell or deploy access to the machine.
"""
