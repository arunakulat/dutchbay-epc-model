# Quick Flake8 Fixes for v14 Commit
## Before Phase 1 – Fix the 13 files

---

## QUICK FIX: F541 (f-string placeholders) – 47 violations

**These are f-strings with no variables. Convert to regular strings.**

### fix_parameter_range_config.py (1 violation)
```python
# Line 238
# BEFORE:
f"some message"

# AFTER:
"some message"
```

### go_with_the_flow_ci_enhanced.py (20 violations)
```python
# Lines: 248, 255, 264, 274, 277, 288(E741), 299, 311, 322, 324, 329, 341, 561, 564, 570, 572, 576, 584, 586, 761

# Find all F541 violations:
grep -n "f\"" go_with_the_flow_ci_enhanced.py | grep -v "f\".*{" | head -30
# (shows f-strings without placeholders)

# Replace pattern:
# f"text" → "text"
# f'text' → 'text'
```

### go_with_the_flow_ci_v2_final.py (20 violations)
Same as above – 20 f-string fixes

### scripts/test_markdown_report.py (5 violations)
Lines: 40, 44, 57, 74, 77

### tests/contracts/test_sensitivity_edgecases.py (2 violations)
Lines: 105, 119

---

## QUICK FIX: E741 (ambiguous variable name 'l')

### go_with_the_flow_ci_enhanced.py (1 violation)
```python
# Line 288
# BEFORE:
for l in some_list:

# AFTER:
for item in some_list:
# (or 'line', 'elem', etc. – anything but 'l')
```

### go_with_the_flow_ci_v2_final.py (1 violation)
```python
# Line 290
# Same fix – rename 'l' to something clear
```

---

## QUICK FIX: F401 (unused imports)

### tests/api/test_scenario_analytics_schema_guard_integration.py
```python
# Remove these imports:
# Line 16: from pathlib import Path  # unused
# Line 18: import pytest             # unused
# Line 21: from analytics.schema_guard import ConfigValidationError  # unused
```

### tests/api/test_scenario_analytics_unit.py
```python
# Line 7: import pytest  # unused
```

### tests/api/test_scenario_analytics_unit_scenario_name.py
```python
# Line 9: from pathlib import Path  # unused
# Line 11: import pytest             # unused
```

---

## QUICK FIX: E501 (line too long)

### scripts/diagnose_capacity_factor.py
```python
# Line 76: 155 characters (limit is 100)
# Break into multiple lines or extract to variable
```

---

## QUICK FIX: F811 (redefinition of unused)

### tests/contracts/test_sensitivity_edgecases.py
```python
# Line 215: Duplicate test function definition
# Remove one of the duplicates:
# def test_range_validation_edge_cases():  # Line 187 (first)
# def test_range_validation_edge_cases():  # Line 215 (duplicate - REMOVE)
```

---

## AUTOMATED APPROACH: Use Autoflake + Manual Edits

```bash
cd ~/dutchbay-epc-model

# 1. Remove unused imports (autoflake handles F401)
autoflake --remove-all-unused-imports --in-place \
  fix_parameter_range_config.py \
  go_with_the_flow_ci_enhanced.py \
  go_with_the_flow_ci_v2_final.py \
  scripts/test_markdown_report.py \
  tests/api/test_scenario_analytics_schema_guard_integration.py \
  tests/api/test_scenario_analytics_unit.py \
  tests/api/test_scenario_analytics_unit_scenario_name.py \
  tests/contracts/test_sensitivity_edgecases.py

# 2. Check what changed
git diff fix_parameter_range_config.py

# 3. Manual fixes needed:
#    - F541: Convert f"text" to "text" (use find/replace in editor)
#    - E741: Rename 'l' to 'item' or 'line'
#    - E501: Break long line
#    - F811: Remove duplicate function
```

---

## USING SUBLIME TEXT (Fast Manual Fix)

1. **Open each file:**
   ```bash
   subl fix_parameter_range_config.py
   ```

2. **Find & Replace F541:**
   - Use Regex: Find `f"([^{]+)"` → Replace `"\1"`
   - This converts `f"text"` → `"text"`

3. **Find & Replace E741:**
   - Find `for l ` → Replace `for item `

4. **Review E501 and F811 manually** (only 2 each)

---

## COMMAND-LINE APPROACH (All Fixes)

```bash
cd ~/dutchbay-epc_model

# 1. Fix F401 (unused imports)
autoflake --remove-all-unused-imports --in-place \
  fix_parameter_range_config.py \
  go_with_the_flow_ci_enhanced.py \
  go_with_the_flow_ci_v2_final.py \
  scripts/test_markdown_report.py \
  tests/api/test_scenario_analytics_schema_guard_integration.py \
  tests/api/test_scenario_analytics_unit.py \
  tests/api/test_scenario_analytics_unit_scenario_name.py \
  tests/contracts/test_sensitivity_edgecases.py

# 2. Fix F541 (f-string placeholders) - use sed for simple cases
# Note: This is tricky with sed; safer to do manually
# For now, flag files that need this:
echo "F541 fixes needed in:"
echo "  - fix_parameter_range_config.py (line 238)"
echo "  - go_with_the_flow_ci_enhanced.py (lines 248, 255, 264, 274, 277, 299, 311, 322, 324, 329, 341, 561, 564, 570, 572, 576, 584, 586, 761)"
echo "  - go_with_the_flow_ci_v2_final.py (20 lines)"
echo "  - scripts/test_markdown_report.py (5 lines)"
echo "  - tests/contracts/test_sensitivity_edgecases.py (2 lines)"

# 3. Fix E741 (ambiguous variable names)
# Manual: Rename 'l' to 'item' in:
#   - go_with_the_flow_ci_enhanced.py (line 288)
#   - go_with_the_flow_ci_v2_final.py (line 290)

# 4. Fix E501 and F811 manually (only 1-2 each)

# 5. Run Black (clean up after edits)
black fix_parameter_range_config.py go_with_the_flow_ci_enhanced.py \
  go_with_the_flow_ci_v2_final.py scripts/test_markdown_report.py \
  tests/api/test_scenario_analytics_schema_guard_integration.py \
  tests/api/test_scenario_analytics_unit.py \
  tests/api/test_scenario_analytics_unit_scenario_name.py \
  tests/contracts/test_sensitivity_edgecases.py

# 6. Run flake8 to verify all fixed
flake8 fix_parameter_range_config.py go_with_the_flow_ci_enhanced.py \
  go_with_the_flow_ci_v2_final.py scripts/test_markdown_report.py \
  tests/api/test_scenario_analytics_schema_guard_integration.py \
  tests/api/test_scenario_analytics_unit.py \
  tests/api/test_scenario_analytics_unit_scenario_name.py \
  tests/contracts/test_sensitivity_edgecases.py

# 7. Try commit again
git add fix_parameter_range_config.py go_with_the_flow_ci_enhanced.py \
  go_with_the_flow_ci_v2_final.py scripts/test_markdown_report.py \
  tests/api/test_scenario_analytics_schema_guard_integration.py \
  tests/api/test_scenario_analytics_unit.py \
  tests/api/test_scenario_analytics_unit_scenario_name.py \
  tests/contracts/test_sensitivity_edgecases.py

git commit -m "feat(v14): Add CI enhancements, lint fixes, and new test suites

[same message as before]"
```

---

## Recommendation: Fastest Path

1. **Run autoflake** (fixes F401 – 6 violations in ~30 sec)
2. **Use Sublime Find & Replace** (fixes F541 – 47 violations in ~3 min with regex)
3. **Manual rename** (fixes E741 – 2 violations in ~1 min)
4. **Manual review** (E501, F811 – 2 violations in ~2 min)
5. **Run Black + flake8** to verify
6. **Commit**

**Total time: ~10 minutes**

---

## Want Me To Help?

I can:
1. Generate exact sed commands for F541 fixes
2. Create a Python script to auto-fix these specific violations
3. Walk you through Sublime Find & Replace patterns

Which approach do you prefer?
