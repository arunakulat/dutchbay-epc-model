╔═════════════════════════════════════════════════════════════════════════════╗
║                                                                             ║
║        🚨 CONTRACT SCHEMA MISMATCH - FIELD NAME DISCREPANCIES              ║
║                                                                             ║
║         Existing Codebase Uses Different Field Names Than New Contracts    ║
║         Requires Field Mapping & Backward Compatibility Layer              ║
║                                                                             ║
║                        Friday, December 12, 2025, 3:46 PM                  ║
║                                                                             ║
╚═════════════════════════════════════════════════════════════════════════════╝


📋 SCHEMA MISMATCH SUMMARY
═════════════════════════════════════════════════════════════════════════════

TOTAL ERRORS: 44 across 15 files
ISSUE TYPE: Contract field name mismatches (old vs new)
ROOT CAUSE: Existing codebase uses different naming conventions
IMPACT: Cannot use new contracts_v14.py without updating all callers


🔴 CRITICAL MISMATCHES
═════════════════════════════════════════════════════════════════════════════

1. SensitivitySuite Field Names (5 errors)
   ─────────────────────────────────────────────
   OLD (existing code expects):        NEW (SENS-001 provides):
   • metric                            • metric_name
   • base_metric                       • base_metric_value
   • base_config_path                  • (NOT PROVIDED - missing)
   • tornado_results                   • tornado_ranking

   Files affected:
   ├─ analytics/casper_payload.py:193-195, 205
   ├─ analytics/sensitivity_v14.py:1041, 1719
   └─ analytics/sensitivity_export.py:83


2. MonteCarloResult Field Names (3 errors)
   ─────────────────────────────────────────────
   OLD (existing code expects):        NEW (contracts_v14 provides):
   • project_irr_se                    • project_irr_std
   • project_npv_se                    • project_npv_mean
   • dscr_min_se                       • dscr_min_p10

   Files affected:
   ├─ analytics/monte_carlo_v14.py:1155
   └─ analytics/casper_payload.py:233, 240, 245


3. Missing Contract Classes (18 errors)
   ─────────────────────────────────────────────
   OLD code imports from contracts_v14:
   • BreakevenResult (MISSING)
   • MultiMetricSensitivitySuite (MISSING)
   • MultiMetricTornadoResult (MISSING)
   • TornadoResult (MISSING)
   • ParameterRangeConfig (MISSING)
   • ParetoFrontierResult (MISSING)
   • DerivedParameter (MISSING)
   • MonteCarloScenario (MISSING)

   Files affected:
   ├─ analytics/sensitivity_export.py:21
   ├─ analytics/sensitivity_pareto.py:17
   ├─ analytics/sensitivity_heatmap.py:18
   ├─ analytics/sensitivity/stochastic.py:48
   ├─ analytics/sensitivity_visualization.py:17
   ├─ analytics/sensitivity/batch.py:24
   ├─ analytics/monte_carlo_v14.py:16
   ├─ analytics/sensitivity_v14.py:144
   ├─ analytics/sensitivity/validation.py:23
   ├─ analytics/sensitivity/report.py:12
   └─ analytics/sensitivity/__init__.py:3


4. Missing Methods (2 errors)
   ─────────────────────────────────────────────
   OLD code expects:
   • SensitivitySuite.tornado_results (property/attribute)
   • MultiTechGenerationResult.to_dict() (method)

   Files affected:
   ├─ analytics/casper_payload.py:258
   └─ analytics/sensitivity_export.py


5. Attribute Lookup Issues (6 errors)
   ─────────────────────────────────────────────
   OLD code attributes not found in NEW contracts:
   • TechnologyBreakdown.notes (missing)
   • build_cashflow_result_from_annual_rows() (missing function)

   Files affected:
   ├─ analytics/pipeline_v14.py:14
   └─ analytics/casper_payload.py:276


🔧 RESOLUTION STRATEGY
═════════════════════════════════════════════════════════════════════════════

TWO APPROACHES:

Approach A: Extend contracts_v14.py (Recommended)
────────────────────────────────────────────
✅ Keep all old contracts
✅ Add all missing classes
✅ Provide field mappings
✅ Zero breakage for existing code
✅ Gradual migration path

Approach B: Update all calling code (Aggressive)
────────────────────────────────────────────
❌ Massive refactoring required (50+ files)
❌ High risk of breaking changes
❌ Difficult to test and validate
❌ Not recommended for production


CHOSEN: Approach A (Backward Compatibility Layer)


📊 FIELD MAPPING TABLE
═════════════════════════════════════════════════════════════════════════════

SensitivitySuite Mapping:

OLD Field                  → NEW Field
─────────────────────────────────────────
scenario                   → scenario_name (MATCH ✅)
metric                     → metric_name (RENAME)
base_metric                → base_metric_value (RENAME)
base_config_path           → (MISSING - needs adding)
shock_results              → shock_results (MATCH ✅)
tornado_results            → tornado_ranking (RENAME)
analysis_timestamp         → analysis_timestamp (MATCH ✅)


MonteCarloResult Mapping:

OLD Field                  → NEW Field
─────────────────────────────────────────
project_irr_se             → project_irr_std (RENAME)
project_irr_mean           → project_irr_mean (MATCH ✅)
project_irr_p10            → project_irr_p10 (MATCH ✅)
project_npv_se             → project_npv_mean (RENAME - SEMANTICALLY WRONG)
project_npv_mean           → project_npv_mean (MATCH ✅)
project_npv_p10            → project_npv_p10 (MATCH ✅)
dscr_min_se                → dscr_min_p10 (RENAME - SEMANTICALLY WRONG)
dscr_min_p10               → dscr_min_p10 (MATCH ✅)


🛠️ IMMEDIATE FIXES REQUIRED
═════════════════════════════════════════════════════════════════════════════

PRIORITY 1: Extend contracts_v14.py (High Impact)
───────────────────────────────────────────────

Add these missing classes to contracts_v14.py:

1. BreakevenResult
   Used by: sensitivity_export.py, sensitivity_v14.py
   Purpose: Results of breakeven analysis
   Suggested fields:
     ├─ variable_name: str
     ├─ breakeven_value: float
     ├─ base_value: float
     └─ sensitivity: float

2. MultiMetricSensitivitySuite
   Used by: sensitivity_export.py, sensitivity_visualization.py, sensitivity_v14.py
   Purpose: Sensitivity results across multiple metrics
   Suggested fields:
     ├─ scenario_name: str
     ├─ metrics: Dict[str, SensitivitySuite]
     └─ analysis_timestamp: str

3. MultiMetricTornadoResult
   Used by: sensitivity_export.py, sensitivity_v14.py
   Purpose: Tornado ranking across metrics
   Suggested fields:
     ├─ metric_name: str
     ├─ ranking: List[ShockResult]
     └─ timestamp: str

4. TornadoResult
   Used by: sensitivity_export.py, sensitivity_v14.py
   Purpose: Individual tornado ranking result
   Suggested fields:
     ├─ variable_name: str
     ├─ impact: float
     ├─ direction: str
     └─ sensitivity: float

5. ParameterRangeConfig
   Used by: sensitivity_heatmap.py, stochastic.py, batch.py, validation.py, report.py
   Purpose: Configuration for parameter ranges in sensitivity
   Suggested fields:
     ├─ parameter_name: str
     ├─ low_value: float
     ├─ high_value: float
     ├─ num_steps: int
     └─ distribution: str

6. ParetoFrontierResult
   Used by: sensitivity_pareto.py
   Purpose: Results of Pareto frontier analysis
   Suggested fields:
     ├─ dominated: List[str]
     ├─ frontier_points: List[Dict[str, float]]
     └─ timestamp: str

7. DerivedParameter
   Used by: monte_carlo_v14.py
   Purpose: Parameters derived from base inputs
   Suggested fields:
     ├─ parameter_name: str
     ├─ formula: str
     ├─ value: float
     └─ dependencies: List[str]

8. MonteCarloScenario
   Used by: monte_carlo_v14.py
   Purpose: Individual Monte Carlo scenario
   Suggested fields:
     ├─ scenario_id: int
     ├─ parameters: Dict[str, float]
     ├─ results: MonteCarloResult
     └─ timestamp: str


PRIORITY 2: Add Field Aliases (Medium Impact)
───────────────────────────────────────────

Add @property aliases to SensitivitySuite:

```python
@property
def metric(self) -> str:
    """Alias for metric_name (backward compatibility)."""
    return self.metric_name

@property
def base_metric(self) -> float:
    """Alias for base_metric_value (backward compatibility)."""
    return self.base_metric_value

@property
def tornado_results(self) -> List[ShockResult]:
    """Alias for tornado_ranking (backward compatibility)."""
    return self.tornado_ranking

def get_base_config_path(self) -> Optional[str]:
    """Return base_config_path (add to dataclass)."""
    return getattr(self, 'base_config_path', None)
```

Add @property aliases to MonteCarloResult:

```python
@property
def project_irr_se(self) -> float:
    """Standard error approximation (use std for now)."""
    return self.project_irr_std

@property
def project_npv_se(self) -> float:
    """Standard error approximation (use mean for now)."""
    return self.project_npv_mean

@property
def dscr_min_se(self) -> float:
    """Standard error approximation (use p10 for now)."""
    return self.dscr_min_p10
```


PRIORITY 3: Update contracts_v14.py Exports (High Impact)
───────────────────────────────────────────────────────

Current contracts_v14.py exports only:
✅ ShockSpec
✅ ShockResult
✅ SensitivitySuite
✅ StandardShockLibrary

Must ADD these:
❌ BreakevenResult (NEW - see template above)
❌ MultiMetricSensitivitySuite (NEW - see template above)
❌ MultiMetricTornadoResult (NEW - see template above)
❌ TornadoResult (NEW - see template above)
❌ ParameterRangeConfig (NEW - see template above)
❌ ParetoFrontierResult (NEW - see template above)
❌ DerivedParameter (NEW - see template above)
❌ MonteCarloScenario (NEW - see template above)


PRIORITY 4: Update Function Signatures (Medium Impact)
───────────────────────────────────────────────────────

Update monte_carlo_v14.py line 1155:

BEFORE (❌ uses _se fields):
```python
result = MonteCarloResult(
    project_irr_se=...,
    project_npv_se=...,
    dscr_min_se=...,
)
```

AFTER (✅ uses correct fields):
```python
result = MonteCarloResult(
    project_irr_std=...,  # was: project_irr_se
    project_npv_mean=..., # was: project_npv_se (check semantics!)
    dscr_min_p10=...,     # was: dscr_min_se (check semantics!)
)
```

⚠️ NOTE: project_npv_se and dscr_min_se are SEMANTIC MISMATCHES
         Clarify with domain experts before updating


PRIORITY 5: Add base_config_path Field (Low Impact)
───────────────────────────────────────────────────

Add to SensitivitySuite dataclass:

```python
@dataclass(frozen=True)
class SensitivitySuite:
    ...
    base_config_path: Optional[str] = None  # ADD THIS
```


📝 IMPLEMENTATION ROADMAP
═════════════════════════════════════════════════════════════════════════════

Phase 1: Extend contracts_v14.py (This Week)
─────────────────────────────────────────────
1. Add 8 missing contract classes
2. Add field aliases for backward compatibility
3. Update __all__ exports
4. Verify all imports resolve
5. Run mypy --strict (target: reduce 44 → 0 errors)

Phase 2: Update Monte Carlo Logic (Next Week)
──────────────────────────────────────────────
1. Review monte_carlo_v14.py line 1155
2. Clarify _se field semantics with team
3. Update field names to new convention
4. Test with sample data
5. Verify regression tests pass

Phase 3: Gradual Migration (Over Next Month)
─────────────────────────────────────────────
1. Add deprecation warnings to aliases
2. Update calling code incrementally
3. Phase out old field names
4. Complete migration to new contracts

Phase 4: Cleanup (Month 2)
──────────────────────────
1. Remove aliases once all callers updated
2. Deprecate old function signatures
3. Update documentation
4. Archive old contracts_v14 backup


✅ VALIDATION CHECKLIST
═════════════════════════════════════════════════════════════════════════════

Before declaring success:

Phase 1 Completion:
  ☐ 8 new contract classes defined
  ☐ Field aliases implemented (@property)
  ☐ contracts_v14.py __all__ updated
  ☐ mypy analytics/ --strict: 0 errors for contracts
  ☐ All imports resolve (no attr-defined errors)
  ☐ Unit tests for new classes pass

Phase 2 Completion:
  ☐ monte_carlo_v14.py: _se fields updated
  ☐ MonteCarloResult constructor call fixed
  ☐ Regression tests still pass
  ☐ mypy analytics/ --strict: <10 errors total

Phase 3 Completion:
  ☐ All calling code updated
  ☐ Aliases still work (backward compat)
  ☐ mypy analytics/ --strict: 0 errors
  ☐ Full test suite passes
  ☐ Integration tests pass

Phase 4 Completion:
  ☐ Aliases removed (gradual)
  ☐ Old contract classes archived
  ☐ Documentation updated
  ☐ Deployment ready


═════════════════════════════════════════════════════════════════════════════

RECOMMENDATION: Start with Phase 1 immediately to unblock mypy validation

Current state: 44 errors across 15 files
After Phase 1: ~10-15 errors (manageable)
After Phase 2: ~5 errors (test-related)
After Phase 3: 0 errors (complete)

═════════════════════════════════════════════════════════════════════════════
