# H03 handover — pytest collection and fixture compatibility

This is the H03 receipt-checkpoint record. It covers only the pytest compatibility
slice of the owner-authorized hygiene queue. At this receipt checkpoint, final-head
rebind, hosted CI, merge and protected-main closeout remain pending. Consult the PR
carrying these records and the external delivery evidence for subsequent live state.
The shared programme ledgers remain controller-owned.

## Startup and frozen source

Use `docs/SESSION_HANDOVER_2026-09-07.md` as the repository bootstrap pointer.
Freshly read the current canonical CSV, AGENTS, all four RECRUIT-01 modules and full
pinned CASPER/CESSPIT/CCCDIR definitions. Confirm project, runtime, current refs and
ownership before any new lease; this handover grants no successor source lease.
The applicable PR1243 handover's professional and HOLD boundaries remain unchanged.

Project origin was the successful DutchBay_EPC_Model project-target creation.
Sole H03 delivery task: `01a0827b-44d2-7660-b7fa-aadec78920c5`.
Worktree: `/Users/aruna/.codex/worktrees/2972/dutchbay-epc-model`.
Branch: `codex/h03-pytest-compatibility`.
Frozen source commit: `1f2c3ca4573fe9f708166461bc389897cc86d1cf`.
Source tree: `a2a233c0acb22aa3bb37e654c6937f14ec92cd34`.
Base and merge-base: `ac75dab100a4cd7509825d9be3f0a43efbbf2a3f`.
Sorted subject manifest: `H03_SUBJECT_MANIFEST.tsv`, SHA-256
`7fd2d391f99b752391d94e40030eb87793d12620d52019ff78c18bc73cffc4e0`.

Actual author and reviewer session metadata verifies `gpt-6-astra` / `xhigh`.
The governed Python is 3.12.13 at
`/Users/aruna/Downloads/Dutchbay_EPC_Model/.venv`; the import probe resolves this
worktree, with no foreign checkout paths. Bootstrap counted 74 active v3.0 rules.
Source leases expire at their verified checkpoint. Reviewers remain read-only.

## Result and evidence

The explicit collection exclusions now include `.hypothesis`, and the class-scoped
FX calibration fixture uses `@classmethod`. Its calculation, source parameters and
class scope remain intact. One pre-existing Black/Ruff layout conflict is resolved
by naming the unchanged assertion diagnostic before the comparison.

The unmodified base reproduced the fixture warning; `-W error` produced two passes
and five fixture-setup errors. A real collection probe with a Hypothesis database
and hostile hidden sentinel failed on the old warning (exit 2) and succeeded with
the new config (exit 0), collecting the visible control and never importing the
hidden sentinel. The full real collection contained 7,810 items both before and
after the compatibility change; all node IDs and marker lists matched exactly,
including all seven qualification exclusions. Full-precision fixture dataclasses
matched, with one setup per class. These claims are bounded to the verified local
environment; unrelated warnings from the original hygiene report remain separate.

On the frozen source commit, the focused strict-warning FX and policy run passed
65 tests in 13.22 seconds, exit 0, no warnings. The command and full receipts appear
in the PR and external `FINAL_LOCAL_CHECK.json`. Scoped Ruff, Black, isort and
pre-commit passed; typed FX dependencies passed mypy (two source files). Local full
suite, coverage, QSTS and qualification campaigns were not run; the native owner
retains local full-suite and environment priority. No dependency, warning filter,
qualification policy, finance implementation or source corpus was changed.

Separate domain and assurance dispositions are preserved in `H03_DOMAIN_REVIEW.md`
and `H03_ASSURANCE_REVIEW.md`. Their original bodies, independent command evidence,
model identity, leases and structured writer receipts are retained externally under
`/Users/aruna/Downloads/DutchBay_Test_Hygiene_2026-09-08/H03/delivery` with hashes.
Both reviewers must rebind any receipt-only final head outside the Git tree after
proving unchanged subject blobs, accurate records and no undispositioned base move.

## Delivery and authority

Before merge, require current-base currency and exact-head success for Test Summary,
fastlane, smoke and Verification receipts (VERIFY-01), including all six shards,
the unchanged 95% coverage floor and hosted Grid Study. The grid policy explicitly
classifies `pyproject.toml` as requiring Grid Study; a skip is insufficient here.
Notify queue and native owners before merge and signal MERGE_SUBMITTED immediately.
Verify squash tree equality and ancestry, synchronize main only with all authorized
clean-index/untracked-config preservation predicates, and retire only H03-owned
clean state after durable evidence preservation. Never delete unfamiliar state.

No issue is linked for automatic closure. Issue #1110 and professional, evidence,
release, lender, Board and other HOLD boundaries remain unchanged. This delivery
closes no other hygiene item and grants no financial or qualification authority.

## Owner standing instructions

Do not wait for approval. work autonomously. Take your best, considered and safe decision. Use the Recruit-01 and verify-01 rules to reconsider and confirm your decisions. Start all new worktrees ingressing the rulesets - GWTF, CESSPIT, CASPER and CCCDIR. Follow these rules rigorously. Copy this instruction to all new tasks, worktrees, PR's

"all new worktrees, PR's etc should also inherit the GPT-6 Astra Extra High setting from the coordinator"
