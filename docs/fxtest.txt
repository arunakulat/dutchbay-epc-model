(.venv311) aruna@Arunas-Mac-mini DutchBay_EPC_Model % # 1. Fix the existing tests in test_schema_guard_fx_validation.py

# 2. Create and run new schema guard tests
pytest tests/analytics/test_schema_guard_fx.py -v

# 3. Run all FX tests
pytest tests/ -k "fx" -v

# 4. Check overall coverage
pytest tests/ -k "fx" --cov=finance.fx_v14 --cov=analytics.schema_guard -v

# 5. Full test suite (ensure no regressions)
pytest tests/ -x  # Stop on first failure

======================================================= test session starts ========================================================
platform darwin -- Python 3.11.14, pytest-9.0.1, pluggy-1.6.0 -- /Users/aruna/Desktop/DutchBay_EPC_Extracted/DutchBay_EPC_Model/.venv311/bin/python3.11
cachedir: .pytest_cache
hypothesis profile 'default'
rootdir: /Users/aruna/Desktop/DutchBay_EPC_Extracted/DutchBay_EPC_Model
configfile: pytest.ini (WARNING: ignoring pytest config in tox.ini!)
plugins: anyio-4.11.0, xdist-3.8.0, hydra-core-1.3.2, hypothesis-6.148.5, cov-7.0.0
collected 0 items                                                                                                                  

====================================================== no tests ran in 0.17s =======================================================
ERROR: file or directory not found: tests/analytics/test_schema_guard_fx.py

======================================================= test session starts ========================================================
platform darwin -- Python 3.11.14, pytest-9.0.1, pluggy-1.6.0 -- /Users/aruna/Desktop/DutchBay_EPC_Extracted/DutchBay_EPC_Model/.venv311/bin/python3.11
cachedir: .pytest_cache
hypothesis profile 'default'
rootdir: /Users/aruna/Desktop/DutchBay_EPC_Extracted/DutchBay_EPC_Model
configfile: pytest.ini (WARNING: ignoring pytest config in tox.ini!)
plugins: anyio-4.11.0, xdist-3.8.0, hydra-core-1.3.2, hypothesis-6.148.5, cov-7.0.0
collected 384 items / 319 deselected / 65 selected                                                                                 

tests/analytics_layer/test_schema_guard_fx.py::test_fx_block_missing PASSED                                                  [  1%]
tests/analytics_layer/test_schema_guard_fx.py::test_fx_block_scalar_mode_valid PASSED                                        [  3%]
tests/analytics_layer/test_schema_guard_fx.py::test_fx_block_structured_mode_valid PASSED                                    [  4%]
tests/analytics_layer/test_schema_guard_fx.py::test_fx_block_legacy_mode_valid PASSED                                        [  6%]
tests/analytics_layer/test_schema_guard_fx.py::test_fx_block_empty PASSED                                                    [  7%]
tests/analytics_layer/test_schema_guard_fx.py::test_fx_block_scalar_negative_rate PASSED                                     [  9%]
tests/analytics_layer/test_schema_guard_fx.py::test_fx_block_structured_extreme_escalation PASSED                            [ 10%]
tests/analytics_layer/test_schema_guard_fx.py::test_fx_block_not_a_dict PASSED                                               [ 12%]
tests/api/test_epc_helper_v14.py::test_epc_breakdown_basic_with_fx_in_config PASSED                                          [ 13%]
tests/api/test_epc_helper_v14.py::test_epc_breakdown_uses_default_fx_when_config_missing_fx PASSED                           [ 15%]
tests/api/test_fx_resolver_unit.py::test_resolve_fx_missing_fx_key_raises PASSED                                             [ 16%]
tests/api/test_fx_resolver_unit.py::test_resolve_fx_scalar_fx_rejected PASSED                                                [ 18%]
tests/api/test_fx_resolver_unit.py::test_resolve_fx_non_mapping_rejected PASSED                                              [ 20%]
tests/api/test_fx_resolver_unit.py::test_resolve_fx_missing_start_key_rejected PASSED                                        [ 21%]
tests/api/test_fx_resolver_unit.py::test_resolve_fx_non_numeric_start_rejected PASSED                                        [ 23%]
tests/api/test_fx_resolver_unit.py::test_resolve_fx_non_numeric_annual_depr_rejected PASSED                                  [ 24%]
tests/api/test_fx_resolver_unit.py::test_resolve_fx_happy_path_defaults_annual_depr_to_zero PASSED                           [ 26%]
tests/api/test_fx_resolver_unit.py::test_resolve_fx_happy_path_with_explicit_annual_depr PASSED                              [ 27%]
tests/api/test_schema_guard_fx_validation.py::test_schema_guard_rejects_missing_fx PASSED                                    [ 29%]
tests/api/test_schema_guard_fx_validation.py::test_schema_guard_rejects_fx_missing_start_rate PASSED                         [ 30%]
tests/api/test_schema_guard_fx_validation.py::test_schema_guard_accepts_valid_fx_mapping PASSED                              [ 32%]
tests/api/test_schema_guard_fx_validation.py::test_schema_guard_accepts_fx_with_zero_depreciation PASSED                     [ 33%]
tests/finance/test_fx_v14.py::test_scalar_curve_basic PASSED                                                                 [ 35%]
tests/finance/test_fx_v14.py::test_scalar_curve_single_year PASSED                                                           [ 36%]
tests/finance/test_fx_v14.py::test_scalar_curve_zero_years PASSED                                                            [ 38%]
tests/finance/test_fx_v14.py::test_scalar_invalid_rate_negative PASSED                                                       [ 40%]
tests/finance/test_fx_v14.py::test_scalar_invalid_rate_zero PASSED                                                           [ 41%]
tests/finance/test_fx_v14.py::test_scalar_invalid_years_negative PASSED                                                      [ 43%]
tests/finance/test_fx_v14.py::test_structured_curve_positive_escalation PASSED                                               [ 44%]
tests/finance/test_fx_v14.py::test_structured_curve_negative_escalation PASSED                                               [ 46%]
tests/finance/test_fx_v14.py::test_structured_curve_zero_escalation PASSED                                                   [ 47%]
tests/finance/test_fx_v14.py::test_structured_invalid_base_rate_negative PASSED                                              [ 49%]
tests/finance/test_fx_v14.py::test_structured_invalid_base_rate_zero PASSED                                                  [ 50%]
tests/finance/test_fx_v14.py::test_structured_invalid_years_negative PASSED                                                  [ 52%]
tests/finance/test_fx_v14.py::test_config_builder_scalar_mode PASSED                                                         [ 53%]
tests/finance/test_fx_v14.py::test_config_builder_structured_mode PASSED                                                     [ 55%]
tests/finance/test_fx_v14.py::test_config_builder_invalid_empty PASSED                                                       [ 56%]
tests/finance/test_fx_v14.py::test_config_builder_invalid_partial PASSED                                                     [ 58%]
tests/finance/test_fx_v14.py::test_get_fx_rate_valid_indices PASSED                                                          [ 60%]
tests/finance/test_fx_v14.py::test_get_fx_rate_negative_index PASSED                                                         [ 61%]
tests/finance/test_fx_v14.py::test_get_fx_rate_index_too_large PASSED                                                        [ 63%]
tests/finance/test_fx_v14.py::test_extreme_escalation_positive PASSED                                                        [ 64%]
tests/finance/test_fx_v14.py::test_extreme_escalation_negative PASSED                                                        [ 66%]
tests/finance/test_fx_v14.py::test_large_project_life PASSED                                                                 [ 67%]
tests/finance/test_fx_v14.py::test_floating_point_precision PASSED                                                           [ 69%]
tests/test_fx_config_strictness.py::test_fx_mapping_with_start_rate_is_accepted PASSED                                       [ 70%]
tests/test_fx_config_strictness.py::test_scenarios_use_mapping_fx PASSED                                                     [ 72%]
tests/test_fx_config_strictness.py::test_scalar_fx_is_rejected_with_clear_error PASSED                                       [ 73%]
tests/test_schema_guard_fx.py::TestFXValidationRejections::test_scalar_fx_is_rejected FAILED                                 [ 75%]
tests/test_schema_guard_fx.py::TestFXValidationRejections::test_missing_fx_section_is_rejected FAILED                        [ 76%]

============================================================= FAILURES =============================================================
______________________________________ TestFXValidationRejections.test_scalar_fx_is_rejected _______________________________________

self = <test_schema_guard_fx.TestFXValidationRejections object at 0x1154598d0>

    def test_scalar_fx_is_rejected(self) -> None:
        """v14 must reject scalar `fx` configs (legacy v13 style)."""
        cfg: dict[str, Any] = {
            "project": {"name": "bad_fx"},
            "fx": 375.0,  # Scalar instead of mapping
        }
    
        with pytest.raises(
            ConfigValidationError,
            match="scalar `fx` is no longer supported",
        ):
>           validate_config_for_v14(
                raw_config=cfg,
                config_path="bad_scalar_fx.yaml",
                modules=["cashflow"],
            )

tests/test_schema_guard_fx.py:59: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

raw_config = {'fx': 375.0, 'project': {'name': 'bad_fx'}}, config_path = 'bad_scalar_fx.yaml', modules = ['cashflow']

    def validate_config_for_v14(
        raw_config: Mapping[str, Any],
        config_path: str | None = None,
        modules: Sequence[str] | None = None,
    ) -> None:
        """
        Validate raw config against registered field specs for v14.
    
        This is the MAIN validation entry point for the v14 stack. It:
        1. Validates FX configuration (always required)
        2. Imports requested modules to trigger schema registration
        3. Validates registered required fields
        4. Raises ConfigValidationError if any checks fail
    
        Parameters
        ----------
        raw_config : Mapping[str, Any]
            Configuration dictionary produced by scenario_loader.
    
        config_path : str | None, optional
            Human-friendly identifier for errors (filename or label).
    
        modules : Sequence[str] | None, optional
            Logical module names to validate against.
            Examples: ["cashflow"], ["cashflow", "debt"]
            If None, only FX validation is performed.
    
        Raises
        ------
        ConfigValidationError
            If required fields are missing or invalid.
    
        Examples
        --------
        >>> config = load_scenario_config("scenario.yaml")
        >>> validate_config_for_v14(config, "scenario.yaml", ["cashflow", "debt"])
        # Raises ConfigValidationError if validation fails
    
        Notes
        -----
        - FX validation is ALWAYS performed (required for v14)
        - Module-specific validation is optional based on `modules` parameter
        - Only ERROR-severity fields are enforced (warnings are logged)
        """
        errors: list[str] = []
    
        # ──────────────────────────────────────────────────────────────────────────
        # Step 1: Validate FX section (always required for v14)
        # ──────────────────────────────────────────────────────────────────────────
    
        _validate_fx_section(raw_config, errors)
    
        # ──────────────────────────────────────────────────────────────────────────
        # Step 2: Ensure modules are imported so they can register field specs
        # ──────────────────────────────────────────────────────────────────────────
    
        module_list = list(modules) if modules is not None else []
    
        for name in module_list:
            _ensure_module_registered(name)
    
        # ──────────────────────────────────────────────────────────────────────────
        # Step 3: Collect and validate registered specs for requested modules
        # ──────────────────────────────────────────────────────────────────────────
    
        specs: list[Any] = []
        for name in module_list:
            specs.extend(get_required_fields(name))
    
        for spec in specs:
            logical_name: str = str(getattr(spec, "name", ""))
            paths: Sequence[PathSpec] = getattr(spec, "paths", ()) or ()
            required: bool = bool(getattr(spec, "required", True))
            validator = getattr(spec, "validator", None)
            severity: str = str(getattr(spec, "severity", SEVERITY_ERROR)).lower()
    
            # Only hard-enforce error-level fields here
            if severity != SEVERITY_ERROR:
                continue
    
            value = _first_resolved_value(raw_config, paths)
    
            ok = True
    
            # Check if required field is missing
            if required and value is None:
                ok = False
    
            # Run custom validator if present
            if ok and validator is not None:
                try:
                    if not validator(value):
                        ok = False
                except Exception as e:
                    logger.debug(
                        "Validator for '%s' raised %s: %s",
                        logical_name,
                        type(e).__name__,
                        e,
                    )
                    ok = False
    
            # Record error if validation failed
            if not ok:
                path_labels = [".".join(p) for p in paths] or ["<unknown>"]
                errors.append(f"{logical_name} (paths: {', '.join(path_labels)})")
    
        # ──────────────────────────────────────────────────────────────────────────
        # Step 4: Raise aggregate error if any checks failed
        # ──────────────────────────────────────────────────────────────────────────
    
        if errors:
            header = (
                f"Config '{config_path}' is missing or has invalid required fields: "
                if config_path
                else "Config is missing or has invalid required fields: "
            )
            details = "; ".join(sorted(errors))
>           raise ConfigValidationError(header + details)
E           analytics.schema_guard.ConfigValidationError: Config 'bad_scalar_fx.yaml' is missing or has invalid required fields: capacity_factor (paths: project.capacity_factor_pct, project.capacity_factor, parameters.capacity_factor_pct, parameters.capacity_factor, capacity_factor_pct, capacity_factor); capacity_mw (paths: project.capacity_mw, project.capacity, parameters.capacity_mw, parameters.capacity); corporate_tax_rate (paths: tax.corporate_tax_rate_pct, tax.corporate_tax_rate, project.corporate_tax_rate_pct, project.corporate_tax_rate, parameters.corporate_tax_rate_pct, parameters.corporate_tax_rate, corporate_tax_rate_pct, corporate_tax_rate); epc_usd_total (paths: capex.usd_total, capex.epc_usd, finance.capex_usd); fx block must be a mapping/dict, got float; fx_start_lkr_per_usd (paths: fx.start_lkr_per_usd); opex_usd_per_year (paths: opex.usd_per_year, opex.usd_annual, opex.annual_opex_usd, costs.opex_usd_per_year, parameters.opex_usd_per_year, opex_usd_per_year); project_life_years (paths: project.project_life_years, project.life_years, parameters.project_life_years, Financing_Terms.tenor_years); tariff_lkr_per_kwh (paths: tariff.lkr_per_kwh, tariff.lkr_kwh, tariff.tariff_lkr_per_kwh, revenue.tariff_lkr_per_kwh, parameters.tariff_lkr_per_kwh, parameters.tariff_lkr, tariff_lkr_per_kwh, tariff_lkr, tariff)

analytics/schema_guard.py:493: ConfigValidationError

During handling of the above exception, another exception occurred:

self = <test_schema_guard_fx.TestFXValidationRejections object at 0x1154598d0>

    def test_scalar_fx_is_rejected(self) -> None:
        """v14 must reject scalar `fx` configs (legacy v13 style)."""
        cfg: dict[str, Any] = {
            "project": {"name": "bad_fx"},
            "fx": 375.0,  # Scalar instead of mapping
        }
    
>       with pytest.raises(
            ConfigValidationError,
            match="scalar `fx` is no longer supported",
        ):
E       AssertionError: Regex pattern did not match.
E         Expected regex: 'scalar `fx` is no longer supported'
E         Actual message: "Config 'bad_scalar_fx.yaml' is missing or has invalid required fields: capacity_factor (paths: project.capacity_factor_pct, project.capacity_factor, parameters.capacity_factor_pct, parameters.capacity_factor, capacity_factor_pct, capacity_factor); capacity_mw (paths: project.capacity_mw, project.capacity, parameters.capacity_mw, parameters.capacity); corporate_tax_rate (paths: tax.corporate_tax_rate_pct, tax.corporate_tax_rate, project.corporate_tax_rate_pct, project.corporate_tax_rate, parameters.corporate_tax_rate_pct, parameters.corporate_tax_rate, corporate_tax_rate_pct, corporate_tax_rate); epc_usd_total (paths: capex.usd_total, capex.epc_usd, finance.capex_usd); fx block must be a mapping/dict, got float; fx_start_lkr_per_usd (paths: fx.start_lkr_per_usd); opex_usd_per_year (paths: opex.usd_per_year, opex.usd_annual, opex.annual_opex_usd, costs.opex_usd_per_year, parameters.opex_usd_per_year, opex_usd_per_year); project_life_years (paths: project.project_life_years, project.life_years, parameters.project_life_years, Financing_Terms.tenor_years); tariff_lkr_per_kwh (paths: tariff.lkr_per_kwh, tariff.lkr_kwh, tariff.tariff_lkr_per_kwh, revenue.tariff_lkr_per_kwh, parameters.tariff_lkr_per_kwh, parameters.tariff_lkr, tariff_lkr_per_kwh, tariff_lkr, tariff)"

tests/test_schema_guard_fx.py:55: AssertionError
__________________________________ TestFXValidationRejections.test_missing_fx_section_is_rejected __________________________________

self = <test_schema_guard_fx.TestFXValidationRejections object at 0x115440910>

    def test_missing_fx_section_is_rejected(self) -> None:
        """FX section is mandatory in v14."""
        cfg: dict[str, Any] = {
            "project": {"name": "no_fx"},
            # No 'fx' key at all
        }
    
        with pytest.raises(
            ConfigValidationError,
            match="Missing `fx` section",
        ):
>           validate_config_for_v14(cfg, "no_fx.yaml", modules=["cashflow"])

tests/test_schema_guard_fx.py:76: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

raw_config = {'project': {'name': 'no_fx'}}, config_path = 'no_fx.yaml', modules = ['cashflow']

    def validate_config_for_v14(
        raw_config: Mapping[str, Any],
        config_path: str | None = None,
        modules: Sequence[str] | None = None,
    ) -> None:
        """
        Validate raw config against registered field specs for v14.
    
        This is the MAIN validation entry point for the v14 stack. It:
        1. Validates FX configuration (always required)
        2. Imports requested modules to trigger schema registration
        3. Validates registered required fields
        4. Raises ConfigValidationError if any checks fail
    
        Parameters
        ----------
        raw_config : Mapping[str, Any]
            Configuration dictionary produced by scenario_loader.
    
        config_path : str | None, optional
            Human-friendly identifier for errors (filename or label).
    
        modules : Sequence[str] | None, optional
            Logical module names to validate against.
            Examples: ["cashflow"], ["cashflow", "debt"]
            If None, only FX validation is performed.
    
        Raises
        ------
        ConfigValidationError
            If required fields are missing or invalid.
    
        Examples
        --------
        >>> config = load_scenario_config("scenario.yaml")
        >>> validate_config_for_v14(config, "scenario.yaml", ["cashflow", "debt"])
        # Raises ConfigValidationError if validation fails
    
        Notes
        -----
        - FX validation is ALWAYS performed (required for v14)
        - Module-specific validation is optional based on `modules` parameter
        - Only ERROR-severity fields are enforced (warnings are logged)
        """
        errors: list[str] = []
    
        # ──────────────────────────────────────────────────────────────────────────
        # Step 1: Validate FX section (always required for v14)
        # ──────────────────────────────────────────────────────────────────────────
    
        _validate_fx_section(raw_config, errors)
    
        # ──────────────────────────────────────────────────────────────────────────
        # Step 2: Ensure modules are imported so they can register field specs
        # ──────────────────────────────────────────────────────────────────────────
    
        module_list = list(modules) if modules is not None else []
    
        for name in module_list:
            _ensure_module_registered(name)
    
        # ──────────────────────────────────────────────────────────────────────────
        # Step 3: Collect and validate registered specs for requested modules
        # ──────────────────────────────────────────────────────────────────────────
    
        specs: list[Any] = []
        for name in module_list:
            specs.extend(get_required_fields(name))
    
        for spec in specs:
            logical_name: str = str(getattr(spec, "name", ""))
            paths: Sequence[PathSpec] = getattr(spec, "paths", ()) or ()
            required: bool = bool(getattr(spec, "required", True))
            validator = getattr(spec, "validator", None)
            severity: str = str(getattr(spec, "severity", SEVERITY_ERROR)).lower()
    
            # Only hard-enforce error-level fields here
            if severity != SEVERITY_ERROR:
                continue
    
            value = _first_resolved_value(raw_config, paths)
    
            ok = True
    
            # Check if required field is missing
            if required and value is None:
                ok = False
    
            # Run custom validator if present
            if ok and validator is not None:
                try:
                    if not validator(value):
                        ok = False
                except Exception as e:
                    logger.debug(
                        "Validator for '%s' raised %s: %s",
                        logical_name,
                        type(e).__name__,
                        e,
                    )
                    ok = False
    
            # Record error if validation failed
            if not ok:
                path_labels = [".".join(p) for p in paths] or ["<unknown>"]
                errors.append(f"{logical_name} (paths: {', '.join(path_labels)})")
    
        # ──────────────────────────────────────────────────────────────────────────
        # Step 4: Raise aggregate error if any checks failed
        # ──────────────────────────────────────────────────────────────────────────
    
        if errors:
            header = (
                f"Config '{config_path}' is missing or has invalid required fields: "
                if config_path
                else "Config is missing or has invalid required fields: "
            )
            details = "; ".join(sorted(errors))
>           raise ConfigValidationError(header + details)
E           analytics.schema_guard.ConfigValidationError: Config 'no_fx.yaml' is missing or has invalid required fields: Missing fx block in config (required for v14); capacity_factor (paths: project.capacity_factor_pct, project.capacity_factor, parameters.capacity_factor_pct, parameters.capacity_factor, capacity_factor_pct, capacity_factor); capacity_mw (paths: project.capacity_mw, project.capacity, parameters.capacity_mw, parameters.capacity); corporate_tax_rate (paths: tax.corporate_tax_rate_pct, tax.corporate_tax_rate, project.corporate_tax_rate_pct, project.corporate_tax_rate, parameters.corporate_tax_rate_pct, parameters.corporate_tax_rate, corporate_tax_rate_pct, corporate_tax_rate); epc_usd_total (paths: capex.usd_total, capex.epc_usd, finance.capex_usd); fx_start_lkr_per_usd (paths: fx.start_lkr_per_usd); opex_usd_per_year (paths: opex.usd_per_year, opex.usd_annual, opex.annual_opex_usd, costs.opex_usd_per_year, parameters.opex_usd_per_year, opex_usd_per_year); project_life_years (paths: project.project_life_years, project.life_years, parameters.project_life_years, Financing_Terms.tenor_years); tariff_lkr_per_kwh (paths: tariff.lkr_per_kwh, tariff.lkr_kwh, tariff.tariff_lkr_per_kwh, revenue.tariff_lkr_per_kwh, parameters.tariff_lkr_per_kwh, parameters.tariff_lkr, tariff_lkr_per_kwh, tariff_lkr, tariff)

analytics/schema_guard.py:493: ConfigValidationError

During handling of the above exception, another exception occurred:

self = <test_schema_guard_fx.TestFXValidationRejections object at 0x115440910>

    def test_missing_fx_section_is_rejected(self) -> None:
        """FX section is mandatory in v14."""
        cfg: dict[str, Any] = {
            "project": {"name": "no_fx"},
            # No 'fx' key at all
        }
    
>       with pytest.raises(
            ConfigValidationError,
            match="Missing `fx` section",
        ):
E       AssertionError: Regex pattern did not match.
E         Expected regex: 'Missing `fx` section'
E         Actual message: "Config 'no_fx.yaml' is missing or has invalid required fields: Missing fx block in config (required for v14); capacity_factor (paths: project.capacity_factor_pct, project.capacity_factor, parameters.capacity_factor_pct, parameters.capacity_factor, capacity_factor_pct, capacity_factor); capacity_mw (paths: project.capacity_mw, project.capacity, parameters.capacity_mw, parameters.capacity); corporate_tax_rate (paths: tax.corporate_tax_rate_pct, tax.corporate_tax_rate, project.corporate_tax_rate_pct, project.corporate_tax_rate, parameters.corporate_tax_rate_pct, parameters.corporate_tax_rate, corporate_tax_rate_pct, corporate_tax_rate); epc_usd_total (paths: capex.usd_total, capex.epc_usd, finance.capex_usd); fx_start_lkr_per_usd (paths: fx.start_lkr_per_usd); opex_usd_per_year (paths: opex.usd_per_year, opex.usd_annual, opex.annual_opex_usd, costs.opex_usd_per_year, parameters.opex_usd_per_year, opex_usd_per_year); project_life_years (paths: project.project_life_years, project.life_years, parameters.project_life_years, Financing_Terms.tenor_years); tariff_lkr_per_kwh (paths: tariff.lkr_per_kwh, tariff.lkr_kwh, tariff.tariff_lkr_per_kwh, revenue.tariff_lkr_per_kwh, parameters.tariff_lkr_per_kwh, parameters.tariff_lkr, tariff_lkr_per_kwh, tariff_lkr, tariff)"

tests/test_schema_guard_fx.py:72: AssertionError
===================================================== short test summary info ======================================================
FAILED tests/test_schema_guard_fx.py::TestFXValidationRejections::test_scalar_fx_is_rejected - AssertionError: Regex pattern did not match.
FAILED tests/test_schema_guard_fx.py::TestFXValidationRejections::test_missing_fx_section_is_rejected - AssertionError: Regex pattern did not match.
!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!! stopping after 2 failures !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
=========================================== 2 failed, 48 passed, 319 deselected in 3.02s ===========================================
======================================================= test session starts ========================================================
platform darwin -- Python 3.11.14, pytest-9.0.1, pluggy-1.6.0 -- /Users/aruna/Desktop/DutchBay_EPC_Extracted/DutchBay_EPC_Model/.venv311/bin/python3.11
cachedir: .pytest_cache
hypothesis profile 'default'
rootdir: /Users/aruna/Desktop/DutchBay_EPC_Extracted/DutchBay_EPC_Model
configfile: pytest.ini (WARNING: ignoring pytest config in tox.ini!)
plugins: anyio-4.11.0, xdist-3.8.0, hydra-core-1.3.2, hypothesis-6.148.5, cov-7.0.0
collected 384 items / 319 deselected / 65 selected                                                                                 

tests/analytics_layer/test_schema_guard_fx.py::test_fx_block_missing PASSED                                                  [  1%]
tests/analytics_layer/test_schema_guard_fx.py::test_fx_block_scalar_mode_valid PASSED                                        [  3%]
tests/analytics_layer/test_schema_guard_fx.py::test_fx_block_structured_mode_valid PASSED                                    [  4%]
tests/analytics_layer/test_schema_guard_fx.py::test_fx_block_legacy_mode_valid PASSED                                        [  6%]
tests/analytics_layer/test_schema_guard_fx.py::test_fx_block_empty PASSED                                                    [  7%]
tests/analytics_layer/test_schema_guard_fx.py::test_fx_block_scalar_negative_rate PASSED                                     [  9%]
tests/analytics_layer/test_schema_guard_fx.py::test_fx_block_structured_extreme_escalation PASSED                            [ 10%]
tests/analytics_layer/test_schema_guard_fx.py::test_fx_block_not_a_dict PASSED                                               [ 12%]
tests/api/test_epc_helper_v14.py::test_epc_breakdown_basic_with_fx_in_config PASSED                                          [ 13%]
tests/api/test_epc_helper_v14.py::test_epc_breakdown_uses_default_fx_when_config_missing_fx PASSED                           [ 15%]
tests/api/test_fx_resolver_unit.py::test_resolve_fx_missing_fx_key_raises PASSED                                             [ 16%]
tests/api/test_fx_resolver_unit.py::test_resolve_fx_scalar_fx_rejected PASSED                                                [ 18%]
tests/api/test_fx_resolver_unit.py::test_resolve_fx_non_mapping_rejected PASSED                                              [ 20%]
tests/api/test_fx_resolver_unit.py::test_resolve_fx_missing_start_key_rejected PASSED                                        [ 21%]
tests/api/test_fx_resolver_unit.py::test_resolve_fx_non_numeric_start_rejected PASSED                                        [ 23%]
tests/api/test_fx_resolver_unit.py::test_resolve_fx_non_numeric_annual_depr_rejected PASSED                                  [ 24%]
tests/api/test_fx_resolver_unit.py::test_resolve_fx_happy_path_defaults_annual_depr_to_zero PASSED                           [ 26%]
tests/api/test_fx_resolver_unit.py::test_resolve_fx_happy_path_with_explicit_annual_depr PASSED                              [ 27%]
tests/api/test_schema_guard_fx_validation.py::test_schema_guard_rejects_missing_fx PASSED                                    [ 29%]
tests/api/test_schema_guard_fx_validation.py::test_schema_guard_rejects_fx_missing_start_rate PASSED                         [ 30%]
tests/api/test_schema_guard_fx_validation.py::test_schema_guard_accepts_valid_fx_mapping PASSED                              [ 32%]
tests/api/test_schema_guard_fx_validation.py::test_schema_guard_accepts_fx_with_zero_depreciation PASSED                     [ 33%]
tests/finance/test_fx_v14.py::test_scalar_curve_basic PASSED                                                                 [ 35%]
tests/finance/test_fx_v14.py::test_scalar_curve_single_year PASSED                                                           [ 36%]
tests/finance/test_fx_v14.py::test_scalar_curve_zero_years PASSED                                                            [ 38%]
tests/finance/test_fx_v14.py::test_scalar_invalid_rate_negative PASSED                                                       [ 40%]
tests/finance/test_fx_v14.py::test_scalar_invalid_rate_zero PASSED                                                           [ 41%]
tests/finance/test_fx_v14.py::test_scalar_invalid_years_negative PASSED                                                      [ 43%]
tests/finance/test_fx_v14.py::test_structured_curve_positive_escalation PASSED                                               [ 44%]
tests/finance/test_fx_v14.py::test_structured_curve_negative_escalation PASSED                                               [ 46%]
tests/finance/test_fx_v14.py::test_structured_curve_zero_escalation PASSED                                                   [ 47%]
tests/finance/test_fx_v14.py::test_structured_invalid_base_rate_negative PASSED                                              [ 49%]
tests/finance/test_fx_v14.py::test_structured_invalid_base_rate_zero PASSED                                                  [ 50%]
tests/finance/test_fx_v14.py::test_structured_invalid_years_negative PASSED                                                  [ 52%]
tests/finance/test_fx_v14.py::test_config_builder_scalar_mode PASSED                                                         [ 53%]
tests/finance/test_fx_v14.py::test_config_builder_structured_mode PASSED                                                     [ 55%]
tests/finance/test_fx_v14.py::test_config_builder_invalid_empty PASSED                                                       [ 56%]
tests/finance/test_fx_v14.py::test_config_builder_invalid_partial PASSED                                                     [ 58%]
tests/finance/test_fx_v14.py::test_get_fx_rate_valid_indices PASSED                                                          [ 60%]
tests/finance/test_fx_v14.py::test_get_fx_rate_negative_index PASSED                                                         [ 61%]
tests/finance/test_fx_v14.py::test_get_fx_rate_index_too_large PASSED                                                        [ 63%]
tests/finance/test_fx_v14.py::test_extreme_escalation_positive PASSED                                                        [ 64%]
tests/finance/test_fx_v14.py::test_extreme_escalation_negative PASSED                                                        [ 66%]
tests/finance/test_fx_v14.py::test_large_project_life PASSED                                                                 [ 67%]
tests/finance/test_fx_v14.py::test_floating_point_precision PASSED                                                           [ 69%]
tests/test_fx_config_strictness.py::test_fx_mapping_with_start_rate_is_accepted PASSED                                       [ 70%]
tests/test_fx_config_strictness.py::test_scenarios_use_mapping_fx PASSED                                                     [ 72%]
tests/test_fx_config_strictness.py::test_scalar_fx_is_rejected_with_clear_error PASSED                                       [ 73%]
tests/test_schema_guard_fx.py::TestFXValidationRejections::test_scalar_fx_is_rejected FAILED                                 [ 75%]
tests/test_schema_guard_fx.py::TestFXValidationRejections::test_missing_fx_section_is_rejected FAILED                        [ 76%]

============================================================= FAILURES =============================================================
______________________________________ TestFXValidationRejections.test_scalar_fx_is_rejected _______________________________________

self = <test_schema_guard_fx.TestFXValidationRejections object at 0x114cf7e10>

    def test_scalar_fx_is_rejected(self) -> None:
        """v14 must reject scalar `fx` configs (legacy v13 style)."""
        cfg: dict[str, Any] = {
            "project": {"name": "bad_fx"},
            "fx": 375.0,  # Scalar instead of mapping
        }
    
        with pytest.raises(
            ConfigValidationError,
            match="scalar `fx` is no longer supported",
        ):
>           validate_config_for_v14(
                raw_config=cfg,
                config_path="bad_scalar_fx.yaml",
                modules=["cashflow"],
            )

tests/test_schema_guard_fx.py:59: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

raw_config = {'fx': 375.0, 'project': {'name': 'bad_fx'}}, config_path = 'bad_scalar_fx.yaml', modules = ['cashflow']

    def validate_config_for_v14(
        raw_config: Mapping[str, Any],
        config_path: str | None = None,
        modules: Sequence[str] | None = None,
    ) -> None:
        """
        Validate raw config against registered field specs for v14.
    
        This is the MAIN validation entry point for the v14 stack. It:
        1. Validates FX configuration (always required)
        2. Imports requested modules to trigger schema registration
        3. Validates registered required fields
        4. Raises ConfigValidationError if any checks fail
    
        Parameters
        ----------
        raw_config : Mapping[str, Any]
            Configuration dictionary produced by scenario_loader.
    
        config_path : str | None, optional
            Human-friendly identifier for errors (filename or label).
    
        modules : Sequence[str] | None, optional
            Logical module names to validate against.
            Examples: ["cashflow"], ["cashflow", "debt"]
            If None, only FX validation is performed.
    
        Raises
        ------
        ConfigValidationError
            If required fields are missing or invalid.
    
        Examples
        --------
        >>> config = load_scenario_config("scenario.yaml")
        >>> validate_config_for_v14(config, "scenario.yaml", ["cashflow", "debt"])
        # Raises ConfigValidationError if validation fails
    
        Notes
        -----
        - FX validation is ALWAYS performed (required for v14)
        - Module-specific validation is optional based on `modules` parameter
        - Only ERROR-severity fields are enforced (warnings are logged)
        """
        errors: list[str] = []
    
        # ──────────────────────────────────────────────────────────────────────────
        # Step 1: Validate FX section (always required for v14)
        # ──────────────────────────────────────────────────────────────────────────
    
        _validate_fx_section(raw_config, errors)
    
        # ──────────────────────────────────────────────────────────────────────────
        # Step 2: Ensure modules are imported so they can register field specs
        # ──────────────────────────────────────────────────────────────────────────
    
        module_list = list(modules) if modules is not None else []
    
        for name in module_list:
            _ensure_module_registered(name)
    
        # ──────────────────────────────────────────────────────────────────────────
        # Step 3: Collect and validate registered specs for requested modules
        # ──────────────────────────────────────────────────────────────────────────
    
        specs: list[Any] = []
        for name in module_list:
            specs.extend(get_required_fields(name))
    
        for spec in specs:
            logical_name: str = str(getattr(spec, "name", ""))
            paths: Sequence[PathSpec] = getattr(spec, "paths", ()) or ()
            required: bool = bool(getattr(spec, "required", True))
            validator = getattr(spec, "validator", None)
            severity: str = str(getattr(spec, "severity", SEVERITY_ERROR)).lower()
    
            # Only hard-enforce error-level fields here
            if severity != SEVERITY_ERROR:
                continue
    
            value = _first_resolved_value(raw_config, paths)
    
            ok = True
    
            # Check if required field is missing
            if required and value is None:
                ok = False
    
            # Run custom validator if present
            if ok and validator is not None:
                try:
                    if not validator(value):
                        ok = False
                except Exception as e:
                    logger.debug(
                        "Validator for '%s' raised %s: %s",
                        logical_name,
                        type(e).__name__,
                        e,
                    )
                    ok = False
    
            # Record error if validation failed
            if not ok:
                path_labels = [".".join(p) for p in paths] or ["<unknown>"]
                errors.append(f"{logical_name} (paths: {', '.join(path_labels)})")
    
        # ──────────────────────────────────────────────────────────────────────────
        # Step 4: Raise aggregate error if any checks failed
        # ──────────────────────────────────────────────────────────────────────────
    
        if errors:
            header = (
                f"Config '{config_path}' is missing or has invalid required fields: "
                if config_path
                else "Config is missing or has invalid required fields: "
            )
            details = "; ".join(sorted(errors))
>           raise ConfigValidationError(header + details)
E           analytics.schema_guard.ConfigValidationError: Config 'bad_scalar_fx.yaml' is missing or has invalid required fields: capacity_factor (paths: project.capacity_factor_pct, project.capacity_factor, parameters.capacity_factor_pct, parameters.capacity_factor, capacity_factor_pct, capacity_factor); capacity_mw (paths: project.capacity_mw, project.capacity, parameters.capacity_mw, parameters.capacity); corporate_tax_rate (paths: tax.corporate_tax_rate_pct, tax.corporate_tax_rate, project.corporate_tax_rate_pct, project.corporate_tax_rate, parameters.corporate_tax_rate_pct, parameters.corporate_tax_rate, corporate_tax_rate_pct, corporate_tax_rate); epc_usd_total (paths: capex.usd_total, capex.epc_usd, finance.capex_usd); fx block must be a mapping/dict, got float; fx_start_lkr_per_usd (paths: fx.start_lkr_per_usd); opex_usd_per_year (paths: opex.usd_per_year, opex.usd_annual, opex.annual_opex_usd, costs.opex_usd_per_year, parameters.opex_usd_per_year, opex_usd_per_year); project_life_years (paths: project.project_life_years, project.life_years, parameters.project_life_years, Financing_Terms.tenor_years); tariff_lkr_per_kwh (paths: tariff.lkr_per_kwh, tariff.lkr_kwh, tariff.tariff_lkr_per_kwh, revenue.tariff_lkr_per_kwh, parameters.tariff_lkr_per_kwh, parameters.tariff_lkr, tariff_lkr_per_kwh, tariff_lkr, tariff)

analytics/schema_guard.py:493: ConfigValidationError

During handling of the above exception, another exception occurred:

self = <test_schema_guard_fx.TestFXValidationRejections object at 0x114cf7e10>

    def test_scalar_fx_is_rejected(self) -> None:
        """v14 must reject scalar `fx` configs (legacy v13 style)."""
        cfg: dict[str, Any] = {
            "project": {"name": "bad_fx"},
            "fx": 375.0,  # Scalar instead of mapping
        }
    
>       with pytest.raises(
            ConfigValidationError,
            match="scalar `fx` is no longer supported",
        ):
E       AssertionError: Regex pattern did not match.
E         Expected regex: 'scalar `fx` is no longer supported'
E         Actual message: "Config 'bad_scalar_fx.yaml' is missing or has invalid required fields: capacity_factor (paths: project.capacity_factor_pct, project.capacity_factor, parameters.capacity_factor_pct, parameters.capacity_factor, capacity_factor_pct, capacity_factor); capacity_mw (paths: project.capacity_mw, project.capacity, parameters.capacity_mw, parameters.capacity); corporate_tax_rate (paths: tax.corporate_tax_rate_pct, tax.corporate_tax_rate, project.corporate_tax_rate_pct, project.corporate_tax_rate, parameters.corporate_tax_rate_pct, parameters.corporate_tax_rate, corporate_tax_rate_pct, corporate_tax_rate); epc_usd_total (paths: capex.usd_total, capex.epc_usd, finance.capex_usd); fx block must be a mapping/dict, got float; fx_start_lkr_per_usd (paths: fx.start_lkr_per_usd); opex_usd_per_year (paths: opex.usd_per_year, opex.usd_annual, opex.annual_opex_usd, costs.opex_usd_per_year, parameters.opex_usd_per_year, opex_usd_per_year); project_life_years (paths: project.project_life_years, project.life_years, parameters.project_life_years, Financing_Terms.tenor_years); tariff_lkr_per_kwh (paths: tariff.lkr_per_kwh, tariff.lkr_kwh, tariff.tariff_lkr_per_kwh, revenue.tariff_lkr_per_kwh, parameters.tariff_lkr_per_kwh, parameters.tariff_lkr, tariff_lkr_per_kwh, tariff_lkr, tariff)"

tests/test_schema_guard_fx.py:55: AssertionError
__________________________________ TestFXValidationRejections.test_missing_fx_section_is_rejected __________________________________

self = <test_schema_guard_fx.TestFXValidationRejections object at 0x114cdf350>

    def test_missing_fx_section_is_rejected(self) -> None:
        """FX section is mandatory in v14."""
        cfg: dict[str, Any] = {
            "project": {"name": "no_fx"},
            # No 'fx' key at all
        }
    
        with pytest.raises(
            ConfigValidationError,
            match="Missing `fx` section",
        ):
>           validate_config_for_v14(cfg, "no_fx.yaml", modules=["cashflow"])

tests/test_schema_guard_fx.py:76: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

raw_config = {'project': {'name': 'no_fx'}}, config_path = 'no_fx.yaml', modules = ['cashflow']

    def validate_config_for_v14(
        raw_config: Mapping[str, Any],
        config_path: str | None = None,
        modules: Sequence[str] | None = None,
    ) -> None:
        """
        Validate raw config against registered field specs for v14.
    
        This is the MAIN validation entry point for the v14 stack. It:
        1. Validates FX configuration (always required)
        2. Imports requested modules to trigger schema registration
        3. Validates registered required fields
        4. Raises ConfigValidationError if any checks fail
    
        Parameters
        ----------
        raw_config : Mapping[str, Any]
            Configuration dictionary produced by scenario_loader.
    
        config_path : str | None, optional
            Human-friendly identifier for errors (filename or label).
    
        modules : Sequence[str] | None, optional
            Logical module names to validate against.
            Examples: ["cashflow"], ["cashflow", "debt"]
            If None, only FX validation is performed.
    
        Raises
        ------
        ConfigValidationError
            If required fields are missing or invalid.
    
        Examples
        --------
        >>> config = load_scenario_config("scenario.yaml")
        >>> validate_config_for_v14(config, "scenario.yaml", ["cashflow", "debt"])
        # Raises ConfigValidationError if validation fails
    
        Notes
        -----
        - FX validation is ALWAYS performed (required for v14)
        - Module-specific validation is optional based on `modules` parameter
        - Only ERROR-severity fields are enforced (warnings are logged)
        """
        errors: list[str] = []
    
        # ──────────────────────────────────────────────────────────────────────────
        # Step 1: Validate FX section (always required for v14)
        # ──────────────────────────────────────────────────────────────────────────
    
        _validate_fx_section(raw_config, errors)
    
        # ──────────────────────────────────────────────────────────────────────────
        # Step 2: Ensure modules are imported so they can register field specs
        # ──────────────────────────────────────────────────────────────────────────
    
        module_list = list(modules) if modules is not None else []
    
        for name in module_list:
            _ensure_module_registered(name)
    
        # ──────────────────────────────────────────────────────────────────────────
        # Step 3: Collect and validate registered specs for requested modules
        # ──────────────────────────────────────────────────────────────────────────
    
        specs: list[Any] = []
        for name in module_list:
            specs.extend(get_required_fields(name))
    
        for spec in specs:
            logical_name: str = str(getattr(spec, "name", ""))
            paths: Sequence[PathSpec] = getattr(spec, "paths", ()) or ()
            required: bool = bool(getattr(spec, "required", True))
            validator = getattr(spec, "validator", None)
            severity: str = str(getattr(spec, "severity", SEVERITY_ERROR)).lower()
    
            # Only hard-enforce error-level fields here
            if severity != SEVERITY_ERROR:
                continue
    
            value = _first_resolved_value(raw_config, paths)
    
            ok = True
    
            # Check if required field is missing
            if required and value is None:
                ok = False
    
            # Run custom validator if present
            if ok and validator is not None:
                try:
                    if not validator(value):
                        ok = False
                except Exception as e:
                    logger.debug(
                        "Validator for '%s' raised %s: %s",
                        logical_name,
                        type(e).__name__,
                        e,
                    )
                    ok = False
    
            # Record error if validation failed
            if not ok:
                path_labels = [".".join(p) for p in paths] or ["<unknown>"]
                errors.append(f"{logical_name} (paths: {', '.join(path_labels)})")
    
        # ──────────────────────────────────────────────────────────────────────────
        # Step 4: Raise aggregate error if any checks failed
        # ──────────────────────────────────────────────────────────────────────────
    
        if errors:
            header = (
                f"Config '{config_path}' is missing or has invalid required fields: "
                if config_path
                else "Config is missing or has invalid required fields: "
            )
            details = "; ".join(sorted(errors))
>           raise ConfigValidationError(header + details)
E           analytics.schema_guard.ConfigValidationError: Config 'no_fx.yaml' is missing or has invalid required fields: Missing fx block in config (required for v14); capacity_factor (paths: project.capacity_factor_pct, project.capacity_factor, parameters.capacity_factor_pct, parameters.capacity_factor, capacity_factor_pct, capacity_factor); capacity_mw (paths: project.capacity_mw, project.capacity, parameters.capacity_mw, parameters.capacity); corporate_tax_rate (paths: tax.corporate_tax_rate_pct, tax.corporate_tax_rate, project.corporate_tax_rate_pct, project.corporate_tax_rate, parameters.corporate_tax_rate_pct, parameters.corporate_tax_rate, corporate_tax_rate_pct, corporate_tax_rate); epc_usd_total (paths: capex.usd_total, capex.epc_usd, finance.capex_usd); fx_start_lkr_per_usd (paths: fx.start_lkr_per_usd); opex_usd_per_year (paths: opex.usd_per_year, opex.usd_annual, opex.annual_opex_usd, costs.opex_usd_per_year, parameters.opex_usd_per_year, opex_usd_per_year); project_life_years (paths: project.project_life_years, project.life_years, parameters.project_life_years, Financing_Terms.tenor_years); tariff_lkr_per_kwh (paths: tariff.lkr_per_kwh, tariff.lkr_kwh, tariff.tariff_lkr_per_kwh, revenue.tariff_lkr_per_kwh, parameters.tariff_lkr_per_kwh, parameters.tariff_lkr, tariff_lkr_per_kwh, tariff_lkr, tariff)

analytics/schema_guard.py:493: ConfigValidationError

During handling of the above exception, another exception occurred:

self = <test_schema_guard_fx.TestFXValidationRejections object at 0x114cdf350>

    def test_missing_fx_section_is_rejected(self) -> None:
        """FX section is mandatory in v14."""
        cfg: dict[str, Any] = {
            "project": {"name": "no_fx"},
            # No 'fx' key at all
        }
    
>       with pytest.raises(
            ConfigValidationError,
            match="Missing `fx` section",
        ):
E       AssertionError: Regex pattern did not match.
E         Expected regex: 'Missing `fx` section'
E         Actual message: "Config 'no_fx.yaml' is missing or has invalid required fields: Missing fx block in config (required for v14); capacity_factor (paths: project.capacity_factor_pct, project.capacity_factor, parameters.capacity_factor_pct, parameters.capacity_factor, capacity_factor_pct, capacity_factor); capacity_mw (paths: project.capacity_mw, project.capacity, parameters.capacity_mw, parameters.capacity); corporate_tax_rate (paths: tax.corporate_tax_rate_pct, tax.corporate_tax_rate, project.corporate_tax_rate_pct, project.corporate_tax_rate, parameters.corporate_tax_rate_pct, parameters.corporate_tax_rate, corporate_tax_rate_pct, corporate_tax_rate); epc_usd_total (paths: capex.usd_total, capex.epc_usd, finance.capex_usd); fx_start_lkr_per_usd (paths: fx.start_lkr_per_usd); opex_usd_per_year (paths: opex.usd_per_year, opex.usd_annual, opex.annual_opex_usd, costs.opex_usd_per_year, parameters.opex_usd_per_year, opex_usd_per_year); project_life_years (paths: project.project_life_years, project.life_years, parameters.project_life_years, Financing_Terms.tenor_years); tariff_lkr_per_kwh (paths: tariff.lkr_per_kwh, tariff.lkr_kwh, tariff.tariff_lkr_per_kwh, revenue.tariff_lkr_per_kwh, parameters.tariff_lkr_per_kwh, parameters.tariff_lkr, tariff_lkr_per_kwh, tariff_lkr, tariff)"

tests/test_schema_guard_fx.py:72: AssertionError
===================================================== short test summary info ======================================================
FAILED tests/test_schema_guard_fx.py::TestFXValidationRejections::test_scalar_fx_is_rejected - AssertionError: Regex pattern did not match.
FAILED tests/test_schema_guard_fx.py::TestFXValidationRejections::test_missing_fx_section_is_rejected - AssertionError: Regex pattern did not match.
!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!! stopping after 2 failures !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
=========================================== 2 failed, 48 passed, 319 deselected in 2.11s ===========================================
======================================================= test session starts ========================================================
platform darwin -- Python 3.11.14, pytest-9.0.1, pluggy-1.6.0
rootdir: /Users/aruna/Desktop/DutchBay_EPC_Extracted/DutchBay_EPC_Model
configfile: pytest.ini (WARNING: ignoring pytest config in tox.ini!)
plugins: anyio-4.11.0, xdist-3.8.0, hydra-core-1.3.2, hypothesis-6.148.5, cov-7.0.0
collected 384 items                                                                                                                

tests/analytics_layer/test_casper_tail_risk_payload.py .                                                                     [  0%]
tests/analytics_layer/test_casper_tail_risk_summary.py .                                                                     [  0%]
tests/analytics_layer/test_contracts_casper_v14.py ..                                                                        [  1%]
tests/analytics_layer/test_evaluation_casper_tail_risk.py .                                                                  [  1%]
tests/analytics_layer/test_evaluation_v14.py ......F

============================================================= FAILURES =============================================================
_______________________________ TestEvaluateWithOverrides.test_non_mapping_config_raises_type_error ________________________________

self = <analytics_layer.test_evaluation_v14.TestEvaluateWithOverrides object at 0x1112d8050>
mock_load_config = <MagicMock name='load_scenario_config' id='4707523984'>
tmp_path = PosixPath('/private/var/folders/s7/bjx1cll14tl2dnwsg35cnw9w0000gn/T/pytest-of-aruna/pytest-22/test_non_mapping_config_raises0')

    @patch("analytics.evaluation_v14.load_scenario_config")
    def test_non_mapping_config_raises_type_error(
        self,
        mock_load_config,
        tmp_path: Path,
    ) -> None:
        cfg_path = tmp_path / "dummy.yaml"
        cfg_path.write_text("dummy: true", encoding="utf-8")
    
        mock_load_config.return_value = ["not", "a", "mapping"]
    
        with pytest.raises(
            TypeError, match="Expected load_scenario_config to return Mapping"
        ):
>           evaluate_with_overrides(cfg_path)

tests/analytics_layer/test_evaluation_v14.py:145: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

config_path = PosixPath('/private/var/folders/s7/bjx1cll14tl2dnwsg35cnw9w0000gn/T/pytest-of-aruna/pytest-22/test_non_mapping_config_raises0/dummy.yaml')
overrides = None

    def evaluate_with_overrides(
        config_path: str | Path,
        overrides: Mapping[str, Any] | None = None,
        *,
        validation_modules: list[str] | None = None,
    ) -> dict[str, float]:
        """
        Run a single scenario evaluation through the v14 pipeline.
    
        This is the CANONICAL entry point for analytics layers:
          - sensitivity_v14
          - monte_carlo_v14
          - any future evaluation-based tools
    
        CCCDIR / GWTF Compliance
        ------------------------
        * Always loads the scenario via analytics.scenario_loader.load_scenario_config
          (no direct yaml.safe_load or ad-hoc parsing).
        * Accepts optional partial override mappings which are pre-normalized into
          schema-aware shapes and then deep-merged on top of the loaded config using
          _deep_merge_config().
        * Does NOT talk to finance or pipeline modules directly; all evaluation goes
          through _run_pipeline_with_config(), which enforces CESSPIT validation and
          extracts a normalized KPI dict.
    
        CESSPIT Compliance
        ------------------
        * Schema validation, module-level guards, and KPI normalization are delegated
          to _run_pipeline_with_config(), which is the single point of entry to the
          v14 finance engine.
        * Override application is transparent and auditable via logging; keys that do
          not line up with the base schema are warned about, as they are common
          sources of “override is a no-op” bugs.
    
        CASPER Compliance
        -----------------
        * Returns a flat, analytics-ready KPI dict suitable for:
            - deterministic tornado / breakeven (sensitivity_v14)
            - Monte Carlo (monte_carlo_v14)
            - downstream CASPER payload construction
        * Adds a WACC proxy alias metric when available:
            - If the engine returns "discount_rate_used", this function will expose
              a canonical "wacc_proxy" metric equal to that value, so analytics
              layers can treat it as a WACC stand-in without knowing pipeline
              internals.
    
        Parameters
        ----------
        config_path : str | Path
            Path to the v14 scenario configuration file (YAML or JSON).
    
        overrides : Mapping[str, Any] | None, optional
            Partial configuration overrides to apply on top of the loaded config.
            Typical usage (from analytics layers) is to pass nested override dicts
            built via build_nested_override() or flat / dotted keys that are
            normalized here.
    
            Examples
            --------
            * None (no overrides; evaluate base config)
            * {"project": {"capacity_factor": 0.45}}
            * {"tariff": {"lkr_per_kwh": 60.0}}
            * {"debt_ratio": 0.75}           # normalized to Financing_Terms.debt_ratio
            * {"tariff_lkr_per_kwh": 60.0}   # normalized to tariff.lkr_per_kwh
            * {"fx.start_lkr_per_usd": 375}  # dotted path expanded under "fx"
    
        validation_modules : list[str] | None, optional
            Optional list of validation modules to enforce (e.g., ["cashflow", "debt"]).
            If None, _run_pipeline_with_config() will inject its own strict defaults.
    
        Returns
        -------
        dict[str, float]
            Flat KPI dict (e.g. {"project_irr": 0.12, "dscr_min": 1.30, ...}).
    
            If the underlying pipeline exposes "discount_rate_used", this function
            will also include:
                - "wacc_proxy": alias to "discount_rate_used"
    
            Any other KPIs surfaced by the v14 pipeline (e.g. project_irr, equity_irr,
            dscr_min, FX-derived metrics) flow through unchanged.
    
        Raises
        ------
        FileNotFoundError
            If the scenario config file does not exist.
    
        TypeError
            If the loaded config is not a Mapping, or overrides is not a Mapping.
    
        Notes
        -----
        * This function is intentionally thin: all heavy lifting (schema validation,
          pipeline wiring, KPI extraction) is delegated to _run_pipeline_with_config().
        * Override pre-normalization:
            - Rewrites common flat override keys (e.g. "debt_ratio") into schema-aware
              nested overrides before deep-merge.
            - Expands dotted keys whose first segment matches a known top-level block
              (e.g. "fx.start_lkr_per_usd" → cfg["fx"]["start_lkr_per_usd"]).
            - Remaining top-level keys that do not exist in the base config are warned
              about, since they often indicate misaligned parameter paths.
        """
        cfg_path = Path(config_path)
        if not cfg_path.is_file():
            raise FileNotFoundError(f"Scenario config not found: {cfg_path}")
    
        base_config = load_scenario_config(cfg_path)
        if not isinstance(base_config, Mapping):
>           raise TypeError(
                "evaluate_with_overrides expected load_scenario_config to return a "
                f"Mapping, got {type(base_config).__name__}"
            )
E           TypeError: evaluate_with_overrides expected load_scenario_config to return a Mapping, got list

analytics/evaluation_v14.py:355: TypeError

During handling of the above exception, another exception occurred:

self = <analytics_layer.test_evaluation_v14.TestEvaluateWithOverrides object at 0x1112d8050>
mock_load_config = <MagicMock name='load_scenario_config' id='4707523984'>
tmp_path = PosixPath('/private/var/folders/s7/bjx1cll14tl2dnwsg35cnw9w0000gn/T/pytest-of-aruna/pytest-22/test_non_mapping_config_raises0')

    @patch("analytics.evaluation_v14.load_scenario_config")
    def test_non_mapping_config_raises_type_error(
        self,
        mock_load_config,
        tmp_path: Path,
    ) -> None:
        cfg_path = tmp_path / "dummy.yaml"
        cfg_path.write_text("dummy: true", encoding="utf-8")
    
        mock_load_config.return_value = ["not", "a", "mapping"]
    
>       with pytest.raises(
            TypeError, match="Expected load_scenario_config to return Mapping"
        ):
E       AssertionError: Regex pattern did not match.
E         Expected regex: 'Expected load_scenario_config to return Mapping'
E         Actual message: 'evaluate_with_overrides expected load_scenario_config to return a Mapping, got list'

tests/analytics_layer/test_evaluation_v14.py:142: AssertionError
===================================================== short test summary info ======================================================
FAILED tests/analytics_layer/test_evaluation_v14.py::TestEvaluateWithOverrides::test_non_mapping_config_raises_type_error - AssertionError: Regex pattern did not match.
!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!! stopping after 1 failures !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
=================================================== 1 failed, 11 passed in 3.02s ===================================================
(.venv311) aruna@Arunas-Mac-mini DutchBay_EPC_Model % 
