# H07 delivery handover — 2026-09-09

Status at this receipt commit: substantive review records accompany the frozen source;
final-head rebind, required hosted CI and protected merge remain pending. This is a
scope-specific successor; `SESSION_HANDOVER_2026-09-07.md` remains the repository
startup/bootstrap pointer and its professional/HOLD boundaries remain unchanged.

## Source and behavior

Sole writer: task `01a0840e-8e5f-7393-a425-34519b444044`, dedicated worktree
`/Users/aruna/.codex/worktrees/5f4a/dutchbay-epc-model`, branch
`codex/h07-fx-integration`. Frozen source `26dfa0dd4fa54f73a1a82ab968691b2684a7233a`;
tree `2eb450293a26046e3b9dd1795b335f6cc56705ba`; base/merge-base
`c17fc42b36ed93736d88e5cc229b048992c5882e`. The external sorted 18-file subject manifest
SHA-256 is `b2c69a0ccb68b4d056e79d9221c5a0609c78cdd12dde1914980fc925f163eef3`.

The committed-base-case integration now executes and fails explicitly if its scenario
is absent. It compares baseline and the three live sensitivity sweeps with the canonical
evaluation gateway and standard-library scalar regression/population variance. Two
mocked tests now read the live start rate relative to baseline, require known slopes,
and verify analytic variance shares. Two pre-existing unused locals were removed to
pass the touched-file lint check. Production analyzer/gateway, financial mathematics,
scenario assumptions and shared runtime are unchanged.

The spread sweep is measured at reference full hedge for this unhedged scenario.
Coefficient units differ; no raw-slope magnitude ranking is asserted. The reported
mean fit R-squared and shares across different sweep regimes remain conventions,
not a stochastic risk decomposition at one operating point.

## Verification and review

Focused test file, pre-existing real-engine coverage and CLI oracles: 78 passed in 6.70s,
zero skips/warnings. Ruff and format checks pass; scoped pre-commit passes. Focused
mypy checks typed bodies while allowing legacy untyped definitions and unused-ignore
comments. Strict comparison of original and candidate test bytes reports 26 versus 21
errors; every candidate error pre-exists. No strict whole-file success is claimed.
Exact commands/results are retained once in external `CHECKS_001.json` and the PR.

In-memory retired analyzer-driver mutation fails all three repaired tests; stale mock
reads fail both mocked tests. Three passing controls bracket the driver mutation.
Missing committed scenario fails explicitly; foreign-cwd execution passes. The initial
probe's copied globals bypassed the patch seam and caused an unintended missing-path
error; that failed harness and its corrected successor remain preserved externally.
The failure is not counted as guard evidence.

Independent domain execution rejected eight mutant-test cases across five distinct
analyzer variants, plus two guards exercised against the stale-mock variant.
Assurance rejected seven hostile cases. These are case counts, not counts of
distinct mutations. Zero Python warnings is separate from existing runtime model
limitation messages. The original domain wording remains preserved; the domain
reviewer confirms this count clarification at final rebind.

Separate domain and assurance review originals are preserved verbatim externally;
public copies in `H07_DOMAIN_REVIEW_001.md` and `H07_ASSURANCE_REVIEW_001.md` retain their
substantive bytes. Any memory-citation footer is removed only in the public derivative,
with exact byte transformation and original/public hashes recorded. Review evidence
and initial failures are append-only.

## Delivery and remaining authority

Before merge, both reviewers must bind the exact receipt-only final head externally,
verify 18 unchanged subject blobs, accurate receipt content and base continuity. Verify
live public attestation bytes and every required exact-head check: Test Summary,
fastlane, smoke, Verification receipts (VERIFY-01). Keep the 95% gate and all required
shards. H07 has no QSTS acceptance dependency; the governed changed-path classifier
returns `qsts_execution_changed=false`. No local full-suite/native campaign ran.

Recheck the programme native reservation and live owner/controller messages before
merge. Notify the coordinator and native owner, signal MERGE_SUBMITTED immediately,
then verify squash tree equality and expected ancestry. Safely fast-forward primary
main only after the standing untracked-config predicates are freshly proven; preserve
its known config bytes. Retire only the owned clean H07 branch/worktree after durable
bundle/evidence verification. External closeout records establish later delivery state.

All evidence resides under
`/Users/aruna/Downloads/DutchBay_Test_Hygiene_2026-09-08/H07/delivery`.
The queue coordinator and programme controller retain their shared ledgers. No issue
automatically closes, and no professional, release, grade, lender, Board or HOLD
boundary changes.

## Owner instruction and runtime provenance

> Do not wait for approval. work autonomously. Take your best, considered and safe decision. Use the Recruit-01 and verify-01 rules to reconsider and confirm your decisions. Start all new worktrees ingressing the rulesets - GWTF, CESSPIT, CASPER and CCCDIR. Follow these rules rigorously. Copy this instruction to all new tasks, worktrees, PR's

Owner addition: "all new worktrees, PR's etc should also inherit the GPT-6 Astra Extra High setting from the coordinator"

Writer and both reviewer tasks use `gpt-6-astra` / `xhigh`, verified in actual session metadata. Persistent Python3.12.13 and active-worktree imports passed the environment gate; full current governance ingress and bootstrap reported 74 active v3.0 rules.
