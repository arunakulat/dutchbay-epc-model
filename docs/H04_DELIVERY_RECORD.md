# H04 workbook concatenation — delivery record and handover

This record covers one presentation/export compatibility change. At this receipt
commit, substantive review is accepted; final-head rebind, hosted CI and merge are
pending. This is a dated checkpoint, not a claim of completed delivery. Consult
the H04 pull request and the external `CLOSEOUT.json` for subsequent live status.
Repository startup remains `SESSION_HANDOVER_2026-09-07.md`; the applicable
September 8 successors and their existing HOLD boundaries remain in force.

## Frozen subject and independent review

| Field | Value |
|---|---|
| Delivery owner | `01a0829d-1887-78d0-83d4-2a58e9264488` |
| Project and worktree | `DutchBay_EPC_Model`; `/Users/aruna/.codex/worktrees/aaba/dutchbay-epc-model` |
| Branch | `codex/h04-workbook-concat-compatibility` |
| Reviewed commit | `97d597b56e30bd5a3baa2de7b4034f7d0edfd8ff` |
| Reviewed tree | `b4e806988ebf5fb26180c0fb702f85c9d1e3111c` |
| Base and merge base | `71d3c138bd6043d7434b4cfacbfc3e39d3d16cec` |
| Subject manifest | `H04_SUBJECT_MANIFEST.tsv` |
| Manifest SHA-256 | `d6b91822615ea20106406e8d54380762983ba1a01956b23e8c0bdea537e6256e` |
| Risk | `R2_LOAD_BEARING` |

The verbatim domain and assurance dispositions are in `H04_DOMAIN_REVIEW.md` and
`H04_ASSURANCE_REVIEW.md`. Original message bytes are also preserved externally;
the tracked copies add only a terminal newline if necessary. Their separate
reviewer identities, execution metadata, exact probes and limitations are stated
in those records. Both reviewers must independently bind the receipt-only final
head after proving subject-blob identity and accurate receipt content.

The source retains separate KPI/covenant frame inference and concatenation order.
It aligns compatible all-missing Value columns to a populated floating-point peer,
including float32, without removing undefined disclosures. Temporal missing values
remain temporal even inside object columns. No financial computation, global
pandas option, dependency pin or production warning suppression changes.

## Rejected predecessor and residual scope

Initial checkpoint `272ba3918de131cb7ae45f105a8e5bca772444a1` converted temporal NaT
sentinels to numeric NaN. It was rejected before recruitment, preserved in the
external checkpoint bundle, and repaired under scope review 002 and a fresh lease.
The repaired tests reject that original candidate in 48 temporal cases. Earlier
12,321-case numerical evidence did not cover that defect and is not an acceptance
of the rejected checkpoint.

The warning-removal claim concerns the reproduced floating-point workbook path.
The wider 58,081-case writer comparison has zero exact output differences, with
2,052 base warnings and 416 candidate warnings. Artificially populated complex
columns retain existing warnings and output behavior; the comparison is not
universally warning-free. For example, an empty KPI block with covenant values
`0` and `complex(float("nan"), 0)` infers a populated complex128 peer. Both reviewers
accepted this residual outside the documented real-valued financial producer
surface. The mapping projection remains permissive; no runtime rejection of all
artificial complex mappings is claimed. A supported-input warning residue would
require a new repair and review before delivery.

The first extended writer probe stopped because it promoted this existing complex
warning to an error. Its original source remains preserved; the successor diagnostic
records warnings and compares exact frames and scalar sentinels. No production
filter was added. The unrelated Starlette dependency deprecation was attributed
separately to the queue coordinator in `STARLETTE_OBSERVATION.json`.

## Verification receipts and limits

Detailed exact commands, probe hashes and structured results are retained once in
the external `WRITER_CHECKS_002.json` and reviewer execution receipts. The pull
request records the applicable commands and results in the VERIFY-01 template.

| Verification | Result |
|---|---|
| Focused workbook, multitech, reproducibility, solar export and download tests, target warning as error | 217 passed; one unrelated Starlette warning; exit 0 |
| Original numerical boundary matrix on repaired bytes | 12,321 exact comparisons; 556 base warnings, zero candidate warnings and differences |
| Expanded dtype comparison | 58,081 exact comparisons; zero differences; warning scope disclosed above |
| Writer hostile controls | Original warning, combined-frame inference, row-dropping and rejected temporal implementation all fail; unmutated control 145 passed |
| Four real pipeline workbook comparisons | All five frames per scenario and normalized XLSX bytes exactly equal to base |
| Independent domain oracle | 4,624 exact comparisons; four real pipeline frame/Excel-cell comparisons; hostile controls fired |
| Independent assurance oracle | 20,736 exact comparisons; hostile controls fired; 144 pure tests passed before and after mutations |
| Scoped Ruff, Ruff format, Black, isort and pre-commit | All applicable checks passed |
| Mypy on executive workbook | Command passed; existing module `ignore_errors` prevents a strict-coverage claim |
| `git diff --check` | Passed |
| Governed environment and canonical bootstrap | Python 3.12.13, active aaba imports; 74 active v3.0 rules |

The local full suite was not run because the native owner retains that capacity.
No environment change, QSTS execution campaign, financial rebaseline or release
qualification was performed. Source-path classification reports
`qsts_execution_changed=false`; the final hosted classifier must confirm the
governed skip. Every required final-head check remains mandatory: Test Summary,
fastlane, smoke and Verification receipts (VERIFY-01). Hosted sharding and the
95% coverage floor remain unchanged. Reviewer filesystem XLSX/HTTP tests were not
run under their no-writes profiles; those receipts are explicitly writer evidence.

## Persistence and next safe action

Evidence root:
`/Users/aruna/Downloads/DutchBay_Test_Hygiene_2026-09-08/H04/delivery`.
It holds ingress, actual project/model identities, exact source and receipt leases,
the rejected checkpoint, original reviews, probes, structured results and the
verified source checkpoint bundle. The queue coordinator owns shared programme
ledgers; H04 did not edit them. The native owner retains environment/full-suite
priority and all other worktrees remain outside this delivery's ownership.

After receipt insertion, open the PR, obtain both external append-only final-head
rebinds, and verify their exact content hashes again at merge. Merge only with
current base, accepted subject bytes and every required final-head check successful.
Notify coordinator and native owner before merge and signal MERGE_SUBMITTED after
submission. Verify protected merge-tree equality and ancestry, synchronize primary
main only after the standing clean-index/known-config predicates are freshly
proved, and retire only owned clean state. Preserve the external final-head and
closeout receipts before removing this calling worktree. Interruption revokes
continuity and requires fresh ownership/hash reconciliation.

Issue #1110, native issue #1229, and professional, financial, lender, Board,
publication, evidence, release and HOLD boundaries remain unchanged. This record
grants none of those authorities and links no issue for automatic closure.

## Owner instruction and execution provenance

Do not wait for approval. work autonomously. Take your best, considered and safe decision. Use the Recruit-01 and verify-01 rules to reconsider and confirm your decisions. Start all new worktrees ingressing the rulesets - GWTF, CESSPIT, CASPER and CCCDIR. Follow these rules rigorously. Copy this instruction to all new tasks, worktrees, PR's

Owner addition: "all new worktrees, PR's etc should also inherit the GPT-6 Astra Extra High setting from the coordinator"

The writer and both separate read-only reviewers used `gpt-6-astra` / `xhigh`.
Actual session metadata was checked; the creation operation binds the project
origin. All freshly ingressed governance identities are retained externally and
in the substantive review records.
