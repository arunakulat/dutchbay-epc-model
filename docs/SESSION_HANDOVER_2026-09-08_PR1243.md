# Session handover — 2026-09-08: F2/F3 DSCR delivery

This successor covers F2/F3 and PR #1243 only. `SESSION_HANDOVER_2026-09-07.md`
remains the repository startup/bootstrap pointer; its predecessor scope and HOLDs
remain. The later PR1237-specific successor remains applicable to coverage-input
policy and its corrected accepted head. Read the current canonical GWTF CSV,
AGENTS, four RECRUIT-01 modules, full framework definitions and applicable charter
before continuing. Verify project association, governed Python 3.12, worktree
ownership/status and live main/PR/issue state; then execute the startup bootstrap.
Historical checkpoint statements do not establish current delivery state.

## Substantive acceptance and preservation

Sole coordinator/writer task: `01a07e1b-a139-79e0-94fc-4a8d48c37b72`.
Worktree: `/Users/aruna/.codex/worktrees/c971/dutchbay-epc-model`.
Branch: `codex/f2f3-dscr-recovery`.
Accepted implementation: `c9921e47106c915651d20112bd9d01c9432e8cb2`.
Tree: `e67a5c7bcd5c881a148d29bb74e99736dbf03642`.
Protected base/merge-base: `61c5956d0ec23d03b1ac472abf735d6172ef4dbd`.
Sorted 19-file subject manifest: `DOLPHIN_F2F3_SUBJECT_MANIFEST.tsv`, SHA-256
`12de78217336f40f69e0add665d660c2ffdf475d2bbcc4d47be63e0e350c9ec8`.

The independent domain and assurance reviews in
`DOLPHIN_F2F3_DOMAIN_REVIEW_003_ACCEPTED.md` and
`DOLPHIN_F2F3_ASSURANCE_REVIEW_003_ACCEPTED.md` accept that exact object.
Both preceding blocked review rounds are preserved verbatim in the corresponding
001/002 records. All demonstrated missing-field, map-consistency and normalized
sentinel findings are closed by fresh independent execution. The assurance-origin
seven-payload error is explicitly superseded: six payloads are unchanged; the
checksum inventory changes only at the manifest.json digest. The raw blocked
review is preserved rather than silently corrected.

Original source worktree `agent-a3dabbfe309764689` remains preserved at
`8935103594760af103ec3032948dbf136aa040b9`, with twelve local commits and the
original dirty record. A verified full-history bundle, original dirty bytes,
patches and stable 1,486-file inventory are held under programme `f2f3/original`.
No original-source cleanup is authorized by this handover.

## Behavior and validation

The debt DSCR series is positional; appended `dscr_periods` carries explicit
operating-year labels and the per-year fold. Covenant assessment validates those
records against the published sources and consumes their canonical normalized
values. The two-term headline minimum, gearing solve, fees/tax and compact
ScenarioResult contract are preserved. The deprecated raw alias remains a plain
list and retains its warning accessor and documented migration gate.

All 37 scenarios were attempted; 29 evaluated and every KPI value matched the real
base exactly; eight matching schema/YAML failures are enumerated in the reviewed
implementation record. Both reviewers independently reproduced the comparison;
domain independently reconstructed the annual fold from CFADS/fees/service.
CEB headline minima remain 0.9069456485322224 and 0.8724193845452927.
Covenant reporting intentionally changes: capacity-charge becomes three breaches,
years 1–7, FAIL; solar/nightpeak becomes two breaches, years 1–4, REVIEW.
VERSION/package 15.5.0 discloses this consequential contract/covenant change.
No canon rebaseline, release tag, F1, A1 or A2 change is part of this work.

Final focused source run: 458 passed, one warning, 23.48 seconds; mypy passed 185
finance/analytics files; scoped pre-commit passed. Independent final assurance
executed 685 tests, and domain executed 283 plus six contract/identity/grid checks.
Each reviewer independently passed a 125-case undefined-value matrix and killed
its own raw-return mutant with passing controls. The author raw-return experiment
had 17-pass controls and nine failed/eight passed under mutation. Earlier failed
runs and both reviewers' harness corrections remain disclosed in the review chain.

Complete local suite on the accepted source head: 7,773 passed, 18 skipped,
15 warnings in 515.51 seconds, exit0. Command: `PYTHONDONTWRITEBYTECODE=1
PYTHONPATH="$PWD" DUTCHBAY_TEST_MODE=full /Users/aruna/Downloads/Dutchbay_EPC_Model/.venv/bin/python
-m pytest -o addopts='' -p no:cacheprovider --no-cov -q --tb=short`. Local coverage
was not collected; the hosted95% gate remained mandatory and passed on that source
head along with all six shards and Grid Study. Final delivery-head hosted checks
must still rerun after receipt insertion. This single clean native run does not
claim to fix issue #1229.

The preceding df4720c full suite stopped with native exit139 at approximately28%.
The original macOS report moved into DiagnosticReports/Retired; both reviewers
independently verified its hash and LLVM linker frames. The domain's path-lookup
addendum resolves its initial unavailable-source limitation. Original report bytes
are preserved externally as `native_crash_df4720c_original.ips`, SHA-256
`8b7a09b7d3a7f4d85abee64c232c12d372b49f79035bb96cf576cec62b295080`.
Issue #1229 remains OPEN; no specific application test or native-crash cure is
claimed. No dependency, JIT or test-policy workaround was introduced.

## Delivery gate and authority

At this metadata receipt commit, final-head rebind and exact-head hosted CI/merge
remain pending. Consult [PR #1243](https://github.com/arunakulat/dutchbay-epc-model/pull/1243)
for append-only final attestations, exact-head checks and verified delivery state.
Required main checks are Test Summary, fastlane, smoke and Verification receipts
(VERIFY-01), with strict base currency. Hosted Grid Study must succeed because the
source-identity dependency touches its governed path. All shards and the95% coverage
floor remain mandatory. A green partial check listing cannot substitute for an
unreported required check. Both reviewers must verify unchanged subject blobs,
receipt-only delta and accurate dispositions when binding the final delivery head.
After squash merge, verify exact accepted-tree equality and ancestry, safely
synchronize clean main and retire only owned clean recovery state.

No issue is linked for automatic closure. Issue #1110 and all F5-02, professional,
release, lender, Board and evidence HOLDs remain. Empty-row zero-observation/PASS
and the standing ordering test's prefix-as-set limitation are disclosed residuals.
Future alias migration belongs to finance maintenance, and series routing to the
contract coordinator, each under a separate scoped review. Engineering acceptance
or merge grants none of those later authorities.

Durable recovery, leases, checks, complete reviews, manifest and final-head comment
hashes are under `/Users/aruna/Downloads/DutchBay_Oldest_First_Programme_2026-09-07/f2f3`.
The shared programme ledger is owned by the controller and was not edited here.

## Owner standing instructions and task configuration

"Do not wait for approval. work autonomously. Take your best, considered and safe decision. Use the Recruit-01 and verify-01 rules to reconsider and confirm your decisions. Start all new worktrees ingressing the rulesets - GWTF, CESSPIT, CASPER and CCCDIR. Follow these rules rigorously. Copy this instruction to all new tasks, worktrees, PR's"

"all new worktrees, PR's etc should also inherit the GPT-6 Astra Extra High setting from the coordinator"

The coordinator and both independent reviewer tasks use GPT-6 Astra Extra High
(`gpt-6-astra`, `xhigh`). These settings describe the authoring/review tasks and are
recorded as worktree/PR provenance.
