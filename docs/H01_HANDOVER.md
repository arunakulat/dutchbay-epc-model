# H01 wind test integrity — receipt-stage handover

This H01-only successor leaves `SESSION_HANDOVER_2026-09-07.md` as the repository
startup/bootstrap pointer. Start any continuation with fresh AGENTS, complete GWTF,
full canonical frameworks, all four RECRUIT-01 modules, live ownership/base and
persistent-environment checks. An interruption revokes writer continuity.

Sole delivery owner: `01a0822e-34fb-7a82-80b6-81a08be20cec`; coordinator:
`01a07e1b-a139-79e0-94fc-4a8d48c37b72`; project `DutchBay_EPC_Model`.
Worktree: `/Users/aruna/.codex/worktrees/440e/dutchbay-epc-model`.
Branch: `codex/h01-wind-test-integrity`. Python 3.12.13 from the persistent governed venv.
Actual writer and both reviewer session configurations verified `gpt-6-astra` / `xhigh`.

## Accepted source checkpoint

Candidate `acd64af762643dd3e85dd64566711f68f8f33faf`, tree
`4630738c62859e5fd390dcb8a68847db72739641`, protected base and merge-base
`5ec33d32834c18e8c14dff06617ff92fec365828`.
The sorted four-object manifest is `H01_SUBJECT_MANIFEST.tsv`, SHA-256
`8a40ecc0fe9bd348b22ffb827bcf44a86ab0485d02af1e7bf71cfd73e56af158`.
Both `H01_WIND_REVIEW.md` and `H01_ASSURANCE_REVIEW.md` preserve the exact original
transported reviewer bodies between explicit markers, with byte lengths and SHA-256.
The external source-message copies also preserve transport-stripped memory metadata.

The repair removes three broad skip wrappers, checks only true top-level package absence,
and retains all 48 original assertions. The source description now correctly names local
packaged data. No production, dependency, environment, policy or financial behavior changes.

Writer checks: 46 focused tests passed with one existing upstream FutureWarning; scoped
Ruff/format/Black/isort/mypy and applicable pre-commit hooks passed. The new guard against
original source gave 12 failed, 6 passed, no skips. The record-inclusive pre-commit initially
fixed an EOF newline and returned 1; the committed candidate rerun passed cleanly. Earlier
unused-ignore type error was corrected. Full commands and limitations are in
`H01_IMPLEMENTATION.md`; each review records its independently executed checks.

Wind review independently exercised 45 observations plus the historical replay and
local-data sentinel. Assurance independently exercised 25 cases and repeated immutable-base
mutation between 18-pass controls; the mutation returned 12 failed / 6 passed / 0 skipped.
Both confirm assertion preservation and restored controls. Reviewer harness mistakes and
corrections are retained in their original records; they are not candidate failures.
No blocking findings remain for the frozen substantive checkpoint. The upstream warning
and bounded nature of controlled fixtures are accepted residuals within H01.

## Remaining delivery gate at this receipt commit

Only the four named receipt files are added after substantive acceptance. No reviewed
subject blob changes. PR creation, both external final-head rebinds and exact-head hosted
CI are pending at this writing. Find the H01 PR by branch `codex/h01-wind-test-integrity`;
its append-only evidence comments and the external delivery receipts carry subsequent
live state. This historical receipt does not claim a completed merge.

The live required contexts are `Test Summary`, `fastlane`, `smoke`, and
`Verification receipts (VERIFY-01)`, with strict base currency. Hosted six-shard coverage
must enforce the unchanged 95% floor. Grid Study may skip only through the unchanged
fail-closed changed-path classifier; H01 makes no QSTS execution claim.

Before merge, notify coordinator and native owner, verify both reviewer identities/content
and exact final commit/tree/base, ensure all required checks succeed, and verify current
protected base. Immediately send MERGE_SUBMITTED after dispatch, then confirm the squash
merge tree equals the accepted final tree and its parent/ancestry matches the protected
base. Safely synchronize clean primary main and retire only owned clean H01 state after
preserving final recovery and external closeout receipts.

Durable evidence root:
`/Users/aruna/Downloads/DutchBay_Test_Hygiene_2026-09-08/H01/delivery/`.
It holds leases, bootstrap/identity and source manifests, complete reviewer bodies,
reviewer reproduction commands without runtime logs, exact PR/comment bodies and hashes,
and the verified complete-history `candidate.bundle`. The coordinator owns the queue;
this delivery owner does not write its shared ledger. Final closeout is recorded externally
so no recursive commit is needed to attest the merge it describes.

## Scope and authority limits

No local full-suite or five-run native campaign was run; the separately owned `defd`
worktree retains that priority. No environment was modified. Native #1229, programme #1110,
finance, professional, evidence, lender, Board and release/HOLD boundaries remain unchanged.
No issue is automatically closed. H01 closes none of the separately scoped jobs, renderer
or pandas dependency-test hygiene findings. Preserve all unrelated worktrees and state.

# Owner standing instruction — 2026-09-08

Copy the following verbatim into every new task prompt, worktree brief and PR description in this programme, including reviewer recruitment and successor tasks:

> Do not wait for approval. work autonomously. Take your best, considered and safe decision. Use the Recruit-01 and verify-01 rules to reconsider and confirm your decisions. Start all new worktrees ingressing the rulesets - GWTF, CESSPIT, CASPER and CCCDIR. Follow these rules rigorously. Copy this instruction to all new tasks, worktrees, PR's

Apply before substantive work in each new worktree: freshly read the full current canonical GWTF CSV and unabridged canonical CESSPIT, CASPER and CCCDIR definitions; execute applicable bootstrap and record provenance. RECRUIT-01 and VERIFY-01 are decision and verification requirements. Use explicit writer ownership, independent exact-object review, meaningful consequential evidence and negative controls. Proceed autonomously on existing authority and merge on independently accepted exact-head required green. Preserve source evidence and actual professional HOLD boundaries. Copying this instruction is not evidence that ingress or verification occurred.

## Inherited model and reasoning setting

Owner addition, verbatim: "all new worktrees, PR's etc should also inherit the GPT-6 Astra Extra High setting from the coordinator"

All new delivery and reviewer tasks must use model `gpt-6-astra` with Extra High reasoning (`xhigh`). Explicitly set `model=gpt-6-astra` and `thinking=xhigh` when creating or resuming Codex tasks. For collaboration agents explicitly set `model=gpt-6-astra`, `reasoning_effort=xhigh` with permitted context-fork arguments. Include this configuration and the verbatim owner instruction in new task prompts, worktree briefs and PR descriptions. The execution setting belongs to authoring/review tasks; record it in associated worktree and PR provenance. Never silently substitute a model or effort. Active F2F3 was sent this explicit configuration override on 2026-09-08; paused tasks receive it when resumed.
