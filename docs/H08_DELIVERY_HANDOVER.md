# H08 delivery handover — 2026-09-09

Status at this receipt commit: substantive implementation independently accepted;
final-head rebind, hosted CI and protected merge remain pending. The later
append-only PR attestations and external closeout receipt determine delivery
state. This H08-only successor leaves the repository startup pointer in
`SESSION_HANDOVER_2026-09-07.md` and all unrelated programme ownership unchanged.

The accepted source is `00123e4af2f7b8988fb2583fc9a7fc20955d972e`, tree
`e3b931d27385701ad90781aa1677201ca24e9875`, protected base/merge-base
`172af0398e8cd3c8c4622a54504d6d8562cb764b`. Its 31-object
`H08_SUBJECT_MANIFEST.tsv` has SHA-256
`14368d0c70dd725eb69621a274ee3d4943336f8869fb652bfef8b1c88aab9266`.
It includes governing rules/modules, unchanged judged tests and dependencies,
and the substantive implementation record. The earlier five-object manifest
and subsequent expansions are preserved externally, with their corrections.
No source changed during manifest expansion or interrupted-review recovery.

Separate domain and assurance dispositions are in `H08_DOMAIN_REVIEW_001.md`
and `H08_ASSURANCE_REVIEW_001.md`. Both accept this exact substantive object.
Domain independently matched 778 outcomes, killed four source mutants and
challenged the actual H01 tests/guard. Assurance independently matched 450
outcomes, killed six source mutants and replayed H01 and the failed/corrected
ordering harness. Their exact tool programs were extracted by the hashes named
in the dispositions. Both also passed the four focused sourcing test modules:
463 tests, zero skips/warnings with FutureWarning-as-error. Writer execution,
initial check failures and scope limits remain in `H08_IMPLEMENTATION_RECORD.md`.

The provider interrupted both initial review attempts and the owner task with a
usage-limit error. The controller authorized one retry of the same owner and
same reviewers at `gpt-6-astra` / `xhigh`. Fresh source, runtime, upstream oracle,
profile and actual session-metadata checks preceded recovery. The interrupted
attempts remain NO_EVIDENCE for a disposition; the completed successor reviews
retain their independently executed earlier checks with exact provenance.
No model change, credit purchase or reset was used.

## Original and publication preservation

External evidence root:
`/Users/aruna/Downloads/DutchBay_Test_Hygiene_2026-09-08/H08/delivery/`.
Both complete original response bodies, including their mandatory memory-citation
footers, are retained there. Public copies omit only each terminal footer and
normalize the adjacent two newlines to one. Every substantive byte is preserved;
the exact transformation and original/public hashes are recorded separately.

| Role | Original SHA-256 | Public SHA-256 |
|---|---|---|
| Domain | `8265566d4928daaf9a00cf47bd864f76d69348b655eaa0cd11b168bc1efa2b7a` | `d1d5bb089bcc0791f7b30c3739e3221296633cec8da123eb041379ceb5c4e41f` |
| Assurance | `09bb796abd95b5b3a57e5028b3728b0da9e00d2c050e2cd393d9719184bd4f4c` | `7c12d664d3fe7e434b3114408be4589c88a9145c8ae7ac784cb5735c50f91729` |

The first preservation helper expected a `channel` field and correctly stopped
without writing when this provider records `phase=final_answer`. The successor
helper recognized that existing metadata field and extracted the exact bodies.
No original response or initial receipt was overwritten.

## Remaining delivery and recovery controls

The sole owner is task `01a08432-8d23-7e11-a26d-86298cfad8f2`, f779 worktree,
branch `codex/h08-windpowerlib-compatibility`. `RECEIPT_LEASE_001.json` permits
only these four receipt files and expires on verified checkpoint. Both reviewers
must verify all 31 subject blobs unchanged, the complete receipt-only delta,
accurate public review bytes and unchanged base before binding the final head.
Their final attestations belong outside the tree, with content hashes and live
publication verification. Any substantive change requires renewed review.

Open the PR from the final receipt head. Required checks are Test Summary,
fastlane, smoke and Verification receipts (VERIFY-01), with strict base currency.
H08's changed paths were classified outside QSTS by the real governed classifier;
verify the final hosted classification and permitted Grid Study skip. Local full
suite, coverage, qualification and QSTS were not run under native-owner capacity.
The hosted full-suite and coverage gates remain mandatory.

Re-read `MERGE_CAPACITY_NATIVE_RESERVATION.json` immediately before merge. An
actually recorded exact-source/runtime native campaign reservation serializes
protected merges through native delivery or explicit recorded release. Notify
the queue coordinator and native owner before submission and signal
MERGE_SUBMITTED immediately afterward. Verify squash-tree equality and expected
ancestry, synchronize primary main only with every authorized safety predicate
rechecked, and retire only owned clean state. A pre-existing primary untracked
`.codex/config.toml` must retain its recorded bytes; no reset, stash, clean or
configuration write is authorized. The source checkpoint also has a verified
external Git bundle requiring the protected base, SHA-256
`33a5930ef3b90a0ca1a91a986a5bce43e77480574ba9da550cfe3fee8aaf9206`.

No turbine physics, curve data, manufacturer-regex semantics, dependencies,
financial baseline, professional conclusion, issue #1110, release, lender,
Board, evidence or HOLD authority changes. The programme controller owns shared
ledgers; native owner owns shared-runtime/full-campaign capacity.

## Owner instructions and execution provenance

> Do not wait for approval. work autonomously. Take your best, considered and safe decision. Use the Recruit-01 and verify-01 rules to reconsider and confirm your decisions. Start all new worktrees ingressing the rulesets - GWTF, CESSPIT, CASPER and CCCDIR. Follow these rules rigorously. Copy this instruction to all new tasks, worktrees, PR's

Owner setting instruction: "all new worktrees, PR's etc should also inherit the GPT-6 Astra Extra High setting from the coordinator"

Writer and both distinct read-only reviewers have actual original and recovery
session metadata verifying `gpt-6-astra` with `xhigh`. Before any resumed mutation,
re-ingress current authority and full rules/framework definitions, verify project,
runtime, worktree ownership, HEAD/tree/base, status and exact target hashes, then
issue a fresh bounded lease. This dated handover is evidence, not a substitute
for that reconciliation.
