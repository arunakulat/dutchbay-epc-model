# H05 delivery handover — 2026-09-09

Status at receipt insertion: frozen source independently accepted; final-head
rebind, hosted CI and PR merge remain pending. This is an H05-only successor to
the repository startup pointer `SESSION_HANDOVER_2026-09-07.md`. Existing issue1110,
professional, release, lender, Board and other HOLD boundaries remain. Later live
PR state and append-only closeout evidence control delivery claims.

## Bootstrap and ownership

Freshly read active AGENTS, the complete canonical GWTF CSV, pinned unabridged
CASPER/CESSPIT/CCCDIR definitions and all four RECRUIT-01 modules before continuing.
Execute the September7 bootstrap, verify DutchBay_EPC_Model project association,
the persistent Python3.12.13 environment, current origin/main, exact worktree and
ownership. Do not infer continuity from this handover after interruption.

H05 owner: `01a083b5-c21a-7201-9393-65b23817bb8d`.
Coordinator: `01a07e1b-a139-79e0-94fc-4a8d48c37b72`.
Branch: `codex/h05-research-dscr-division`.
Worktree: `/Users/aruna/.codex/worktrees/dfde/dutchbay-epc-model`.
Frozen source: `1cd58c3f021f3e13827e923b42ffbdd6156b15ea`.
Tree: `89a5cdbc6e2df00d6fbe54201691f25ff9718242`.
Protected base/merge-base: `446d595fe526541c549ee90aca61c4698ac0876b`.
Subject manifest: `H05_SUBJECT_MANIFEST.tsv`, SHA-256
`bb8b1322f2e3c765b2081c11c4422cd8bc67f4693a0af419e1d73168170cca82`.

The source and receipt leases, actual task/reviewer metadata, bootstrap source
hashes, original full review bodies, exact probes and concise results are held at
`/Users/aruna/Downloads/DutchBay_Test_Hygiene_2026-09-08/H05/delivery`.
The project-target creation event lives one directory above in
`CREATION_IDENTITY.json`; root/controller independently verified actual sessions.
Writer and both separate reviewers are `gpt-6-astra` / `xhigh`.

## Behavior and evidence

The legacy research DSCR calculation uses masked NumPy division with the unchanged
strict `total_ds > 1e-6` predicate and undefined NaNs. A private helper supports
independent boundary testing. The existing default project object is named in a
private singleton to satisfy B008 while retaining its original lifetime and
mutable-default semantics. Historical assumptions and canonical finance are unchanged.

The author compared default and all-LKR results against real-base source: all
annual cells, dtypes, ordering and all eight scalar outputs were exact, including
both IRRs. The domain reviewer independently added actual no-argument comparison,
a 50-case oracle, discounted-sum checks, default lifetime and four hostile variants.
Assurance independently checked four configurations including a responsive tariff
change, 77 scalar pairs and seven source mutants, including real-model call-site
bypasses. Original bodies and limits are pinned in `H05_DOMAIN_REVIEW.md` and
`H05_ASSURANCE_REVIEW.md`. Every case retained five undefined DSCR years.

## Author checks

All commands used the governed environment and active-checkout imports.

| Check | Exact command | Result |
|---|---|---|
| Research suite | `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$PWD" DUTCHBAY_TEST_MODE=full /Users/aruna/Downloads/Dutchbay_EPC_Model/.venv/bin/python -m pytest -o addopts='' -p no:cacheprovider --no-cov tests/research -q -W error::RuntimeWarning` | 26 passed in 1.87s; exit 0; zero warnings |
| Lint | `/Users/aruna/Downloads/Dutchbay_EPC_Model/.venv/bin/ruff check scripts/research/legacy_v12.py tests/research/test_v12_optimizer.py` | All checks passed; exit 0 |
| Format | `/Users/aruna/Downloads/Dutchbay_EPC_Model/.venv/bin/ruff format --check scripts/research/legacy_v12.py tests/research/test_v12_optimizer.py` | 2 files already formatted; exit 0 |
| Types | `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$PWD" /Users/aruna/Downloads/Dutchbay_EPC_Model/.venv/bin/mypy scripts/research/legacy_v12.py --ignore-missing-imports --allow-untyped-defs --allow-untyped-calls --allow-any-generics --cache-dir=/tmp/h05-mypy-cache` | Success: no issues found in 1 source file; exit 0 |
| Base and negative controls | `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$PWD" /Users/aruna/Downloads/Dutchbay_EPC_Model/.venv/bin/python /Users/aruna/Downloads/DutchBay_Test_Hygiene_2026-09-08/H05/delivery/validate_candidate.py` | 2 exact (20,19) annual frames; all 8 scalar values equal each; 4 mutants killed; 5 restored controls PASS |
| Pre-commit | `DUTCHBAY_VENV=/Users/aruna/Downloads/Dutchbay_EPC_Model/.venv PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$PWD" PATH="/Users/aruna/Downloads/Dutchbay_EPC_Model/.venv/bin:$PATH" pre-commit run --files scripts/research/legacy_v12.py tests/research/test_v12_optimizer.py changelog.d/hygiene-legacy-dscr-division.fixed.md` | All applicable hooks passed; TOML/YAML no matching files |
| Diff | `git diff --check` | exit 0 |

The original strict-warning base test failed as intended. The first explicit-path
lint found pre-existing B008; source correction passes without a lint ignore.
Exact prior commands and the distinction between process-local mutations and no
persistent runtime mutation are recorded in author clarification002. Original
records remain unchanged. Reviewer audit-harness failures and the model-capacity
interruption also remain visible; the same assurance reviewer resumed before
issuing its final acceptance.

Not run: local full suite/coverage, QSTS, qualification, deployment or shared-runtime
qualification. Local full-suite work was deferred to native-owner priority, which
does not itself mean an actual campaign reservation. H05's changed-path policy
result was `qsts_execution_changed:false`; hosted Test Summary still requires the
applicable shards, coverage and mandatory downstream jobs.

## Remaining delivery and cleanup

Open the PR after this receipt insertion. Both original reviewers must rebind to
its final commit/tree/base after verifying all16 reviewed blobs, the receipt-only
delta and accurate representation of their original bodies. Retain each final
attestation append-only outside the tree with content hash and evidence URL; verify
it again at merge. Required contexts currently are Test Summary, fastlane, smoke
and Verification receipts (VERIFY-01), with current-base enforcement. Query live
policy and every exact-head check at delivery.

Reconcile current native reservation with the coordinator/controller and native
owner before merge. If an actual exact-source/runtime campaign reservation exists,
serialize protected-main merges until native delivery or recorded release. No
speculative reservation was recorded at source review. No local full-suite overlap
or shared-environment mutation is authorized by H05.

Notify coordinator and native owner before merge, then send MERGE_SUBMITTED
immediately after submission. Verify protected tree equality with accepted final
head and expected ancestry. Primary main's pre-existing `.codex/config.toml` may
be preserved through already-authorized ff-only sync only after all predicates
are freshly proved: tracked/index clean, HEAD ancestor, incoming paths nonoverlapping,
no competing ownership mutation, and identical config bytes before/after. Expected
config SHA-256 is `42dac680fc1f9e47c5cbc864ca50984445019be946ac09b470981fd25bd2969d`.
Retire only H05's clean owned branch/worktree after preserving recovery evidence.
The external closeout receipt must record actual merge, primary sync and retirement;
this pre-merge handover does not claim those have occurred. Shared programme ledgers
belong to the controller.

## Owner standing instruction and configuration

Do not wait for approval. work autonomously. Take your best, considered and safe decision. Use the Recruit-01 and verify-01 rules to reconsider and confirm your decisions. Start all new worktrees ingressing the rulesets - GWTF, CESSPIT, CASPER and CCCDIR. Follow these rules rigorously. Copy this instruction to all new tasks, worktrees, PR's

"all new worktrees, PR's etc should also inherit the GPT-6 Astra Extra High setting from the coordinator"

Actual writer/reviewer configuration: `gpt-6-astra` / `xhigh`.
