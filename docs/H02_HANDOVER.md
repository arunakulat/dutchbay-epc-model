# H02 dependency tests — receipt-stage handover

This H02-only successor leaves `SESSION_HANDOVER_2026-09-07.md` as the startup
bootstrap pointer. Resume with live AGENTS, complete canonical GWTF, unabridged
frameworks, all four RECRUIT-01 modules, project/environment identity and current
ownership/base checks. Interruption revokes writer continuity. Historical receipt
statements do not establish subsequent PR/merge state.

Owner: task `01a08255-9a4b-74b2-8618-b09b76be0775`; queue coordinator:
`01a07e1b-a139-79e0-94fc-4a8d48c37b72`; project `DutchBay_EPC_Model`.
Worktree `/Users/aruna/.codex/worktrees/61b5/dutchbay-epc-model`;
branch `codex/h02-dependency-guard-tests`.
Writer and both actual reviewer sessions verify `gpt-6-astra` / `xhigh`.
Persistent Python 3.12.13: `/Users/aruna/Downloads/Dutchbay_EPC_Model/.venv`.

## Accepted substantive checkpoint

Candidate `551f06da55996b47b8bd65c478a5e72c5e84f11b`;
tree `769531eed31fd715fcbaf12138a82e73976076a5`;
base/merge-base `f1fba1c0c3f5fdb3147acfee90b6581cfb8ee00f` (H01 #1244).
The five-object sorted `H02_SUBJECT_MANIFEST.tsv` has SHA-256
`7f23b4e9522046e2051816bc8d766b53aa4a9b599f8250f8ae4dec9c57645c0a`.

The real jobs guard/backend now runs with arq and redis separately unavailable;
real PDF loader coverage includes package and native OSError failures; pandas
absence patches the production dependency reference. Guidance/cause assertions and
memory, installed PDF and DataFrame success controls remain. Only tests, changelog
and evidence records change; no production, environment or financial behavior.

`H02_IMPLEMENTATION.md` records the writer's 67-pass expanded focused check,
zero installed-dependency skips, six killed real-code mutants with passing controls,
exact DataFrame restoration and scoped lint/format/types/pre-commit results.
Initial touched-file lint/type failures and their narrow corrections remain disclosed.
No complete local suite, coverage collection or native campaign was performed.

Separate original domain and assurance reviews accept the exact frozen candidate.
Their structured durable representations are `H02_DOMAIN_REVIEW.md` and
`H02_ASSURANCE_REVIEW.md`; exact original bodies, complete source messages and
independent probe inputs are preserved externally with verified hashes. Each
reviewer independently ran 50 tests, zero skips, and its own hostile probes.
The domain killed six mutants; assurance killed five, including an incorrectly
gated memory backend. Both verified import/dependency restoration and real positives.

Both identified an inherited stale preflight-status field in external L003.
The original remains unchanged. Its actual successful clean-status assertion and
fresh HEAD/tree are recorded by `lease003_preflight_addendum.json`; assurance has
verified the correction, and domain must verify it at final rebind. This provenance
correction is not a subject-code or authority change. Parent review-extraction and
reviewer hash-query harness failures remain recorded externally; no failed review
or probe has been silently converted into acceptance.

## Receipt-only delivery and remaining gates

At this receipt commit, only the four named H02 review/manifest/handover files have
been added since substantive acceptance. All five reviewed blobs must remain exact.
PR creation, both append-only external final-head attestations and hosted final-head
CI remain pending at writing. Find the PR by the H02 branch; consult its external
comments and the H02 delivery root for subsequent live state.

The actual required main contexts are `Test Summary`, `fastlane`, `smoke`, and
`Verification receipts (VERIFY-01)`, with strict base currency. All six hosted shards
and the unchanged 95% coverage floor remain mandatory. The unchanged Grid Study
classifier marks the five substantive changed paths unrelated; rerun on the final
changed-path set. No QSTS execution claim is made. A classified skip is not QSTS evidence.

After PR creation, both reviewers must verify subject blob identity, only named
receipt changes, accurate representation of their original disposition and the
external correction before binding final commit/tree/base/manifest. Preserve their
exact attestation bodies and content hashes outside the attested Git tree, publish
append-only evidence comments, then verify comment content and identity at merge.
Any subject change restarts both substantive reviews. Base movement requires the
three recorded module-3 proofs and fresh reviewer binding.

Before merge notify the coordinator and native owner; reverify current protected
base, final-head required green and both attestations. Signal MERGE_SUBMITTED
immediately after submission, then prove protected merge-tree equality and expected
ancestry. Preserve recovery evidence and synchronize primary main only after all
standing-authorized predicates hold: tracked/index clean; HEAD ancestor; incoming
paths nonoverlapping; no competing ownership mutation; pre-existing untracked
`.codex/config.toml` SHA-256
`42dac680fc1f9e47c5cbc864ca50984445019be946ac09b470981fd25bd2969d`
identical before/after. Never reset, stash, clean or rewrite that config.
Retire only owned clean H02 branch/worktree when safe; do not delete the calling
worktree before evidence and verified closeout permit it.

## Persistence and boundaries

Evidence root: `/Users/aruna/Downloads/DutchBay_Test_Hygiene_2026-09-08/H02/delivery/`.
It contains leases, ingress and actual session configuration, writer commands,
structured validations, frozen manifest, verbatim original reviews, independent
probe code and hashes, correction history and later PR/rebind/merge/cleanup receipts.
No routine runtime logs are retained. Final protected-state facts are written there
to avoid recursive commits attesting their own merge.

The programme controller owns shared ledgers. Native task
`01a08180-c2e0-71b1-b80d-54661c802b53` retains environment/full-suite priority;
all other worktrees and original evidence remain outside H02 cleanup authority.
Issue #1110, native #1229, other hygiene items, finance, professional, evidence,
lender, Board, release and HOLD boundaries remain. No issue auto-closure or grade,
publication, deployment or qualification authority is granted.

# Owner standing instruction — 2026-09-08

Copy the following verbatim into every new task prompt, worktree brief and PR description in this programme, including reviewer recruitment and successor tasks:

> Do not wait for approval. work autonomously. Take your best, considered and safe decision. Use the Recruit-01 and verify-01 rules to reconsider and confirm your decisions. Start all new worktrees ingressing the rulesets - GWTF, CESSPIT, CASPER and CCCDIR. Follow these rules rigorously. Copy this instruction to all new tasks, worktrees, PR's

Apply before substantive work in each new worktree: freshly read the full current canonical GWTF CSV and unabridged canonical CESSPIT, CASPER and CCCDIR definitions; execute applicable bootstrap and record provenance. RECRUIT-01 and VERIFY-01 are decision and verification requirements. Use explicit writer ownership, independent exact-object review, meaningful consequential evidence and negative controls. Proceed autonomously on existing authority and merge on independently accepted exact-head required green. Preserve source evidence and actual professional HOLD boundaries. Copying this instruction is not evidence that ingress or verification occurred.

## Inherited model and reasoning setting

Owner addition, verbatim: "all new worktrees, PR's etc should also inherit the GPT-6 Astra Extra High setting from the coordinator"

All new delivery and reviewer tasks must use model `gpt-6-astra` with Extra High reasoning (`xhigh`). Explicitly set `model=gpt-6-astra` and `thinking=xhigh` when creating or resuming Codex tasks. For collaboration agents explicitly set `model=gpt-6-astra`, `reasoning_effort=xhigh` with permitted context-fork arguments. Include this configuration and the verbatim owner instruction in new task prompts, worktree briefs and PR descriptions. The execution setting belongs to authoring/review tasks; record it in associated worktree and PR provenance. Never silently substitute a model or effort. Active F2F3 was sent this explicit configuration override on 2026-09-08; paused tasks receive it when resumed.
