# H06 delivery handover — 2026-09-09

This successor covers only the canonical IRR normalization-overflow hygiene repair.
`SESSION_HANDOVER_2026-09-07.md` remains the startup/bootstrap pointer. Existing
programme, professional and HOLD boundaries are unchanged; historical live-state
claims require fresh verification. No shared programme ledger is edited here.

## Frozen accepted source

- Delivery owner: task `01a083e4-e8c7-7151-aa83-2c6a56fba311`.
- Project: `DutchBay_EPC_Model`; worktree `/Users/aruna/.codex/worktrees/bce7/dutchbay-epc-model`;
  branch `codex/h06-irr-overflow`.
- Candidate: `84beded0d4b460f3b668aeed58c52f244ecbbac2`.
- Tree: `5ecc3d38352d1366334d7d076d45315dbd026a91`.
- Base/merge-base: `3cc3d0ebba1d09c27c17556a2c40ee379904b969`.
- Sorted subject-manifest SHA-256: `8c9d8882c34b1412aa3ffac3b9340594e0dd87149b34f5fe916798ab3a8ff7fb`.

```text
changelog.d/hygiene-irr-overflow.fixed.md	c2624890997a071f35dc0bdf575982ea7dcc627f
finance/irr.py	36ba86b44b94253428f3b2b21cd98c4586604a44
tests/finance/test_irr_property_hypothesis.py	07a8f2de34d34e7cdcfde6d30fa3b538cb697698
```

The domain and assurance receipts in `H06_DOMAIN_REVIEW_2026-09-09.md` and
`H06_ASSURANCE_REVIEW_2026-09-09.md` record separate independent acceptance of this
exact subject. Original bodies and exact tool commands are preserved outside Git.
The author's source leases expire at their verified checkpoints. The receipt lease
allows only these two review records and this handover; source blobs stay frozen.

## Change and verified boundaries

Finite coefficient normalization that necessarily produces an infinite companion
matrix now reaches the original numerical-failure fallback before NumPy emits its
warning. Successful library paths, original cashflows, caller bounds, NPV residual
validation and fallback behavior remain intact. The property generator and original
test bodies are unchanged; its introduction now correctly describes residual checking.
Both independent reviewers accepted numerical neutrality for the inspected governed
implementation and the resulting DOC-02 no-version-bump decision. Reciprocal overflow
is a retained scoped limitation. Future numerical dependency changes need reassessment.

The writer's final source passed 111 focused tests in 1.35 seconds and 1,350 financial
regression tests with one pre-existing optional-pvlib skip in 24.11 seconds. Mypy
passed two changed Python files and the broader 185-file finance/analytics surface;
scoped pre-commit and diff checks passed. Exact commands/results are in the PR and
external `CHECKS_AND_RATIONALE.md` plus `FINAL_SOURCE_BINDING_003.json`.

The final probe reports 1,037 old/new/restored comparisons with no outcome changes,
408 predicted failures all confirmed by actual library errors, 2,051 binary64 pairs
and 18 analytic/bounds controls. Two distinct mutation variants were killed across
five test executions. The reviewers supplied separate rational/Decimal and hostile
oracles, rather than merely rerunning these author tests.

Earlier evidence remains visible: source checkpoint `fd6cf13` precedes the stronger
controlled-root test; a test-only lease produced this final source; its first Black
hook reformatted a lambda and returned 1 before the successful final rerun. Probe v2
records preformat test bytes and is preserved unchanged. V3 and append-only binding
003 verify actual execution on final source; precision receipt 004 distinguishes
mutation variants from test executions. Three actual cubic tiny-root searches did
not reproduce a finite-rate reciprocal-overflow example; the hostile test states
its eigensolver-seam limitation explicitly.

## Remaining delivery gate at this receipt commit

Open the PR after inserting these receipts. Both reviewers must verify unchanged
subject blobs, accurate receipt content and this metadata-only delta, then bind the
exact final commit/tree/base through append-only external evidence. Preserve and hash
the exact originals; verify their content again at merge. No recursive self-SHA claim
is made here. Live PR state and the external closeout receipt govern later status.

Recheck current main and the native campaign reservation before merge. Required
checks are Test Summary, fastlane, smoke and Verification receipts (VERIFY-01);
all six shards and the complete 95% coverage gate remain mandatory. The changed-path
Grid Study classifier returned false for the three source paths; the final hosted
classifier must confirm the final diff. Local full-suite/native campaigns, coverage,
qualification, environment changes and deployment were not run by this owner.

Notify queue coordinator and native owner before merge and immediately on
MERGE_SUBMITTED. Verify accepted-tree equality and parent/ancestry after squash.
Primary main may fast-forward only after fresh tracked/index cleanliness, ancestor,
nonoverlap, no-competing-owner and unchanged pre-existing `.codex/config.toml` hash
proof. Preserve every unrelated worktree. Retire only clean H06 state after a verified
recovery bundle and external closeout; leave the calling worktree until safe removal
is explicitly recorded. Interruption or drift requires fresh ownership/lease checks.

No issue is auto-closed. #1110, native #1229 and professional/lender/Board/release and
other evidence HOLDs remain. Engineering delivery confers none of those authorities.
Evidence root: `/Users/aruna/Downloads/DutchBay_Test_Hygiene_2026-09-08/H06/delivery/`.

## Owner instructions and execution provenance

Do not wait for approval. work autonomously. Take your best, considered and safe decision. Use the Recruit-01 and verify-01 rules to reconsider and confirm your decisions. Start all new worktrees ingressing the rulesets - GWTF, CESSPIT, CASPER and CCCDIR. Follow these rules rigorously. Copy this instruction to all new tasks, worktrees, PR's

"all new worktrees, PR's etc should also inherit the GPT-6 Astra Extra High setting from the coordinator"

Actual author and both read-only reviewer session metadata verify `gpt-6-astra` with
`xhigh`. The project-target creation, runtime, full canonical ingress and bootstrap
are separately recorded; copying this instruction is not evidence of their execution.
