- Config-first grid-outage assumptions lever (#744): a new `grid_outage_pct` (resolved from `project.*` / `parameters.*` / top-level, like `curtailment_pct`) applies an INCREMENTAL grid-unavailability haircut on delivered energy in `_calculate_net_production`, after grid loss and curtailment. Default 0.0 → byte-identical (the embedded availability is already in the bankable net AEP / capacity_factor); a non-zero value is the documented grid-downtime stress/risk input above that base. Distinct from `curtailment_pct` (economic dispatch curtailment) — this is grid unavailability. The `CashflowParams.grid_outage_pct` contract carries the anti-double-count note: do NOT also declare `resource.losses.grid_availability_pct` for the same downtime (that reduces the AEP summary) or the haircut compounds. KPI oracle byte-identical across all 27 committed scenarios (none declares the lever). Turning it on for a committed scenario is a separate KPI-moving change.
