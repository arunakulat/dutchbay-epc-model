- Closed the remaining #683 W1 Fable-gate ledger items: (a) wind-resource knobs (the IEC 61400-15-2 uncertainty budget / P50 haircut and air-density corrections) are now exposable from the `run_full_pipeline_v14.py` CLI via a `resource_overrides` mapping deep-merged onto the scenario's `resource` block — OFF by default (null → byte-identical; the original scenario on disk is never mutated); (d) `LcosResult.as_dict()` keeps `depth_of_discharge` for an energy_tariff BESS but now discloses in the notes that the reported value is inert in that model's LCOS math (the denominator uses full nameplate energy per cycle, not a DoD fraction) — surfaced, not silently dropped; (b) removed the orphan `conf/scenario_analytics_v14.yaml` (zero code references; the live batch base is `conf/run_scenario_analytics_v14.yaml`). All KPI-neutral: oracle byte-identical across all 27 committed scenarios.
