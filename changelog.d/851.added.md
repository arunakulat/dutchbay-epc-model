- **Per-technology resource & technology chapters in the HTML report** (§4/§5, #851) — the resource-assessment (§4) and technology-selection (§5) feasibility sections now render *per active generation technology*. A new OPTIONAL `technology: wind | solar | bess | shared` tag on the config-first section schema (`config/feasibility_sections.yaml` → `analytics.feasibility_sections`) marks which technology a section reads under (backward-compatible: untagged sections behave exactly as before; the tag never gates coverage). The renderer (`app/reports/report_model.py`) emits one sub-chapter per active technology, reusing the multi-tech generation discovery / the genuine-hybrid gate — wind-only carries only the wind chapter, solar-only the solar chapter, a hybrid both (alongside the existing cross-technology reconciliation); a tech's chapter is omitted when that tech is absent. Each chapter surfaces the display-only provenance already resolved elsewhere: the WIND chapter shows the #742 directional wind rose (as a sector-frequency table, no plot dependency), the self-declared terrain class, and the coarse single-cell-ERA5 caveat; the SOLAR chapter shows the #743 source-quality grade, the monofacial-financed-chain bifacial disclosure (SOLAR-9), and the effective flat soiling percent. Pure presentation — KPI-neutral, no economics touched.
