- **Shared RMS ride-through dynamics core (LVRT + HVRT + frequency)** (`analytics/grid/ride_through.py`, `analytics/grid/ride_through_poc.py`, `analytics/contracts_v14.py`, #875 D4a) — generalises the D1 single-LVRT ANDES scaffold (#872) into one parameterised ride-through core that runs the three grid-code fault-ride-through cases — **LVRT** (voltage dip), **HVRT** (voltage swell) and **frequency** excursion — against a generic WECC IBR (REGCA1/REECA1/REPCA1) and reports, per case, whether the ANDES RMS positive-sequence time-domain solve rode the disturbance through. The ride-through envelope (LVRT/HVRT entry pu, k-factors, the continuous under/over-frequency band from the trip table) is parsed from the redacted D0 grid-code fixture (`tests/fixtures/grid/envision_enpcs01_gridcode.yaml`) rather than magic constants. A new frozen advisory contract `RideThroughResult` joins `analytics.contracts_v14` (CCCDIR — result types centralized): per-case pass/fail (`ran`/`converged`), the probed `target_pu`/`target_hz`, `k_factor`, min/max rode-through voltage, `n_devices`, and a dedicated `disclaimer` field carrying the "GENERIC WECC model, NOT the OEM-certified .dyr/.dll" caveat so no downstream emitter can drop it; `bankable=False` always with provenance — never the utility-accepted EMT (PSCAD/EMTP) FRT compliance study. `analytics/grid/ride_through_poc.py` becomes a thin backward-compatibility SHIM: its `run_lvrt_case` / `lvrt_enter_from_fixture` / `_require_andes` delegate to / re-export from the shared core (no behaviour change, no deletion — REFACTOR-01 Dolphin Strategy). CASPER: `andes` is guarded at call-time via `_require_andes()` — never imported at module scope, so the default (grid-free) install imports cleanly. Default-off / dynamic-study gate: the ANDES solve runs ONLY when a caller opts in with `run_dynamics=True`; with the gate off (the default) the core does pure-Python envelope parsing + case set-up and returns a NOT-RUN advisory, so the default screen stays STATIC-only and `andes` is never imported by default even in a `[grid]` environment. The study is ADVISORY — it never feeds the finance engine, so committed-scenario KPIs are byte-identical (project/equity IRR, NPV, min DSCR unchanged). The ANDES LVRT/HVRT/frequency solves run in the opt-in `-m grid` lane with the `[grid]` extra installed; the default suite proves the CASPER import guard, the envelope parsing + case-spec + gate-off assembly (100% grid-free coverage of the new core), and the `RideThroughResult` contract.
