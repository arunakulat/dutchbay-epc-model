- **Wind-rose OUTPUT builder + coastal/terrain siting METADATA** (#742, display/provenance
  only). New `analytics.wind.wind_rose.build_wind_rose(wd_series, n_sectors=12)` bins
  met-convention wind directions (e.g. ERA5 `wd_100m`) into equal-width sectors → directional
  frequency; honestly labelled as coarse single grid-cell ERA5 reanalysis (not mast-validated).
  New `analytics.wind.siting_metadata.resolve_siting_metadata` surfaces an optional, self-declared
  `resource.siting.terrain_class` (default UNSET). Both are surfaced into the AEP-summary
  `provenance` block only via optional `resource.wind_rose` / `resource.siting` config and apply
  **NO AEP correction** — they feed no cashflow or billed quantity (KPI oracle byte-identical).
  A terrain AEP correction factor, and enabling `resource.wake.model_live` on any committed
  scenario, remain deferred to a separate user-gated (KPI-moving) change.
