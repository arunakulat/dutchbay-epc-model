- **ESA WorldCover -> roughness-length (z0) raster** (`analytics.gis.landcover_roughness`, #830) — reclass an ESA WorldCover ~10 m land-cover GeoTIFF to a cited land-cover-class -> z0 (metres) lookup (WMO/Davenport-Wieringa, config-overridable) and export a provenance-stamped z0 GeoTIFF + manifest entry. KPI-neutral: not wired into any AEP/energy calc.
