- Import-derived architecture diagram (#746): `scripts/gen_architecture_diagram.py` walks every first-party source package with `ast` and renders the package-level dependency graph to `docs/architecture_import_graph.mmd`, also spliced (rendered) into `docs/ARCHITECTURE.md`. A `--check` mode plus `tests/docs/test_architecture_diagram.py` fail if the committed diagram drifts from the live import graph, so it stays honest. The other §5a docs deliverables (public-API reference, model-user guide, worked examples, test-linked changelog) are already satisfied by existing docs (`api_contract_casper_result_v1.md`, `ANALYTICS_INTEGRATION.md`, `QUICK_START.md`, `examples/`, the `changelog.d` system) and were not duplicated (R24).
