- **Grid interconnection DATA MODEL + strict CESSPIT schema + `grid_types` + `allow_unvalidated_grid` gate** (`analytics/grid/grid_interface_schema.py`, `finance/grid/grid_types.py`, #873 D2) — EXTENDS the D1 (#872) minimal `grid:` block into the full config-first per-site interconnection contract: structured `poc.{bus_name,nominal_kv,plant_rated_mva}` + `thevenin.{short_circuit_mva_min,short_circuit_mva_max,x_r,assumption_basis}` (strict-required when a `grid` block is present) plus the descriptive `scr.*` / `gridcode.*` envelope and an opt-in per-tech `generation.technologies.<name>.grid` interface (`rated_mva`, `converter_type` GFL/GFM enum, transformer/collector impedance, `dynamic_model {family,params_ref,source}`). New `finance/grid/grid_types.py` is the single source of truth for the GFL/GFM converter-topology + `MODELLED_GRID_TECHS` frozensets and the `allow_unvalidated_grid` opt-in gate (modelled EXACTLY on `allow_unvalidated_flat_cf` in `finance/tech_types.py`): an ESTIMATED Thevenin (`assumption_basis: screening_estimate`) or an enum-only converter interface (no committed `dynamic_model`) is REFUSED at pre-flight unless the block sets `allow_unvalidated_grid: true`, so a user can never silently treat a screening estimate as bankable. Cross-field rules a flat spec cannot express (Thevenin min ≤ max — the IEC-60909 min case governs — and the two opt-in gates) are validated by `validate_grid_block`, wired into the strict `validate_config_for_v14` gatekeeper via a present-only `_validate_grid_conditionals` hook. The canonical `scenarios/dutchbay_lendercase_2025Q4.yaml` gains a DOCUMENTED grid block (Thevenin as a screening estimate + min/max range, `assumption_basis: screening_estimate` + the required opt-in, red-flag provenance note); the new `docs/GRID_INTERFACE_SCHEMA.md` documents every field. DEFAULT-OFF (`grid.study_enabled: false`), advisory-only, no heavy grid dep imported (config/schema only) — committed-scenario KPIs are byte-identical (project/equity IRR, NPV, min DSCR unchanged); grid-absent scenarios never touch the grid validation path.
