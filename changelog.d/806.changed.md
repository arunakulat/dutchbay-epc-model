- Surface `min_dscr_period` (the per-period sculpt floor) alongside the fold-corrected `min_dscr` headline in the executive workbook Ratios sheet, so both the #790 fold minimum and the sculpt floor export. Re-point `scripts/generate_solar_assessment_report.py`'s gearing optimiser to constrain on `min_dscr_period` (the sculpt-floor intent) rather than the fold-corrected headline, and correct the now-stale `finance/debt_v14.py` fold-vs-period comment (they diverge on the lendercase and both CEB BESS scenarios since #790). KPI-neutral (display/label follow-ups from the #790 review; #806).
