- **Artifact path normalization, slice-2 (#735)** — routed the remaining artifact/report/export
  writers through the shared `analytics.output_paths.resolve_output_dir` resolver so every artifact
  of a run co-scopes. In `run_full_pipeline_v14.py` the four report/workbook emitters
  (`executive_workbook.xlsx`, `capital_risk_report.html`, `interaction_grid_report.html`,
  `tech_comparison_report.html`) now derive their DEFAULT paths from a single up-front
  `export_dir_scoped` alongside the JSON/CSV dump block, fixing a latent scatter where run-scoping
  the JSON block left the reports un-scoped; explicit user-supplied paths still take precedence
  unchanged. The Monte-Carlo and sensitivity Hydra CLIs and the batch scenario-analytics entrypoint
  (whose engine writes a sibling `*_charts/` directory) route their output directories through the
  resolver too. Added `DEFAULT_MC_OUTPUT_ROOT` and `DEFAULT_SENSITIVITY_OUTPUT_ROOT` to
  `analytics.output_paths` (CCCDIR single-source). Run-scoping is opt-in via `run_scoped`/`run_id`
  cfg knobs (default off); at the default every path is unchanged, so committed-scenario output
  stays byte-identical.
