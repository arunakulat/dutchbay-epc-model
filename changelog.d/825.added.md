- **Project-boundary polygon clip of wind/AEP rasters** (`analytics.gis.boundary_clip`, #825 — revives falsely-closed #21) — `clip_to_polygon()` masks a single-band GeoTIFF (e.g. a #20 WS150/CF/AEP export) to a project-boundary polygon via `rasterio.mask`, writing a clipped GeoTIFF + `.prov.json` sidecar + `DataLake_Manifest_All.json` entry, plus a clipped-domain stats block (cos(latitude) area-weighted mean / min / max / in-polygon cell count). Ships an **explicitly illustrative** placeholder boundary `wind_resource/config/DutchBay_ProjectArea.geojson` (NOT a surveyed boundary — replace before real use). CASPER-guarded on the `[gis]` extra. KPI-neutral: clipped rasters/stats feed QGIS/lender reporting only, not any AEP/energy/finance calc.
