- **Grid interconnection study scaffold — D0 fixture + `[grid]` extra** (#871) — lay the
  opt-in, default-off groundwork for the hosting-capacity / grid-strength study (D1+).
  Adds a redacted grid-code parameter fixture (`tests/fixtures/grid/
  envision_enpcs01_gridcode.yaml`) parsed from the Envision ENPCS01 GFL PCS dynamic model
  (rating 2750 kW; ±0.95 pf; ±0.5 pu Q; LVRT/HVRT entry + k-factors; frequency & voltage
  trip tables) with a full provenance string — REFERENCED, never executed; the proprietary
  OEM `.dyr`/`.dll`/`.pfd` binaries are not committed. Adds a `[grid]` optional-dependencies
  extra (`pandapower>=3.3,<3.4`, `andes>=2.0`, `opendssdirect.py>=0.9.4`) pinned so the
  resolver cannot silently downgrade the frozen core pins (numpy/scipy/pandas), a `grid`
  pytest marker, and a NON-BLOCKING opt-in CI lane (nightly / manual dispatch only) that is
  the sole lane installing `[grid]` — the default `test` matrix stays grid-free. A tiny
  loader test proves the fixture parses and carries its provenance. No grid libraries are
  imported yet (that is D1). KPI-neutral, add-only, default-off.
