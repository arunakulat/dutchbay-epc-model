- Lender report: two-factor sensitivity **interaction grid** section (#739 slice-2) — a driver-A × driver-B heatmap of a KPI and its interaction (deviation from one-way additivity), surfacing the slice-1 engine primitive. Rendered render-when-present with full honesty disclosure of failed cells, declared-vs-evaluated base mismatch, and unresolved drivers (never hides a failed cell). Opt-in via the `emit_interaction_grid` flag on `run_full_pipeline_v14.py`; the two drivers and metric are config-required (`interaction_grid` block, fail-loud, no default pair imposed). Latency-gated: the N×M full-pipeline grid runs only on the batch/opt-in path — the synchronous API report route is untouched and stays byte-identical (#645), as do all committed scenarios (default-off).
