Track the existing project-scoped Codex defaults in `.codex/config.toml` and guard their exact
approved values so every trusted DutchBay EPC worktree inherits the same approval, sandbox,
verbosity and reasoning-summary settings.
