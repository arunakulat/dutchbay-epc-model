- Public API contract freeze (#841, #788 P1): `app/api/responses.API_CONTRACT_VERSION` (SemVer) versions the client-facing response shape, stamped on every `CaseResult` and surfaced on `GET /health`. `tests/app/test_api_contract.py` pins the public `CaseResult` field set and behaviourally pins the public endpoint surface (auth / run-case / report / sensitivity / async jobs — each must stay routed, not 404), so a breaking change to a response model or the removal of a public endpoint fails loudly rather than silently shipping to the wizard/iOS client. Additive + display-only; KPI-neutral (oracle byte-identical).
