- **GIS-MCDM siting-suitability surface** (`analytics.gis.mcdm_suitability`, #834) — combine the wind-resource (#828), slope (#829) and roughness-z0 (#830) criterion rasters into a `[0, 1]` siting-suitability surface via AHP-weighted Weighted Linear Combination: config-first per-criterion normalisation (benefit/cost direction + breakpoints), AHP pairwise weights with the Saaty Consistency Ratio (flagged when CR > 0.10), WLC combination, then a hard Boolean exclusion-mask (#833) veto. Exports a provenance-stamped suitability GeoTIFF + `.prov.json` + manifest entry via the #20 helpers. KPI-neutral: analytic/GIS output only; not consumed by any AEP/energy/finance calc.
