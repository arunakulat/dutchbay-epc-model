================================================================================
VIOLATION CONTEXT
================================================================================


================================================================================
FILE: analytics/fx/returns.py | LINE: 163
================================================================================
     160:     # Newton-Raphson method
     161:     rate = initial_guess
     162: 
>>>  163:     for iteration in range(max_iterations):
     164:         # Calculate NPV and derivative (NPV')
     165:         npv_val = 0.0
     166:         npv_derivative = 0.0

================================================================================
FILE: analytics/sensitivity_visualization.py | LINE: 40
================================================================================
      37:     [base - low_val for low_val in low]
      38:     [h - base for h in high]
      39:     fig, ax = plt.subplots(figsize=(10, 0.6 * len(df) + 2))
>>>   40:     for i, (b, low_val, h) in enumerate(zip(base, low, high)):
      41:         ax.barh(
      42:             i, b - l, left=l, color="#d35400", height=0.5, label="Low" if i == 0 else ""
      43:         )

================================================================================
FILE: analytics/sensitivity_visualization.py | LINE: 81
================================================================================
      78:     angles = [n / float(len(metrics)) * 2 * np.pi for n in range(len(metrics))]
      79:     angles += angles[:1]
      80:     fig, ax = plt.subplots(figsize=(7, 7), subplot_kw={'polar': True})
>>>   81:     for idx, row in df.iterrows():
      82:         vals = [row[f"Impact_{m}"] for m in metrics] + [row[f"Impact_{metrics[0]}"]]
      83:         ax.plot(angles, vals, linewidth=2, label=row["Parameter"])
      84:         ax.fill(angles, vals, alpha=0.1)

================================================================================
FILE: finance/debt_v14.py | LINE: 198
================================================================================
     195:         total_interest = sum(interest_map.values())
     196:         principal_total = max(0.0, target_service - total_interest)
     197:         total_bal = sum(obals.values()) or 1.0
>>>  198:         for k, tr in tranches.items():
     199:             bal = obals[k]
     200:             prorata = bal / total_bal if total_bal > 0 else 0.0
     201:             principal_k = min(bal, principal_total * prorata)

================================================================================
FILE: tests/test_export_smoke.py | LINE: 58
================================================================================
      55:     assert hasattr(batch_report, "successful")
      56:     assert hasattr(batch_report, "failed")
      57: 
>>>   58:     successful = getattr(batch_report, "successful")
      59:     failed = getattr(batch_report, "failed")
      60: 
      61:     # These are currently lists of scenario_ids / error dicts

================================================================================
FILE: tests/test_export_smoke.py | LINE: 59
================================================================================
      56:     assert hasattr(batch_report, "failed")
      57: 
      58:     successful = getattr(batch_report, "successful")
>>>   59:     failed = getattr(batch_report, "failed")
      60: 
      61:     # These are currently lists of scenario_ids / error dicts
      62:     assert isinstance(successful, list)

================================================================================
FILE: tests/test_scenario_analytics_smoke.py | LINE: 57
================================================================================
      54:     assert hasattr(batch_report, "successful")
      55:     assert hasattr(batch_report, "failed")
      56: 
>>>   57:     successful = getattr(batch_report, "successful")
      58:     failed = getattr(batch_report, "failed")
      59: 
      60:     assert isinstance(successful, list)

================================================================================
FILE: tests/test_scenario_analytics_smoke.py | LINE: 58
================================================================================
      55:     assert hasattr(batch_report, "failed")
      56: 
      57:     successful = getattr(batch_report, "successful")
>>>   58:     failed = getattr(batch_report, "failed")
      59: 
      60:     assert isinstance(successful, list)
      61:     assert isinstance(failed, list)

================================================================================
FILE: analytics/fx/risk.py | LINE: 83
================================================================================
      80:         }
      81: 
      82:     def percentile_analysis(
>>>   83:         self, returns: np.ndarray, percentiles: List[int] = [10, 25, 50, 75, 90]
      84:     ) -> Dict[str, float]:
      85:         """
      86:         Calculate percentile distribution of returns.

================================================================================
FILE: analytics/sensitivity_pareto.py | LINE: 21
================================================================================
      18:     sensitivity_suite: SensitivitySuite,
      19:     base_config_path: str,
      20:     objectives: Sequence[str] = ("project_irr", "dscr_min"),
>>>   21:     constraints: Dict[str, float] = {"project_irr": 0.12, "dscr_min": 1.3},
      22:     steps: int = 6,
      23: ) -> ParetoFrontierResult:
      24:     """