# PDF report rendering (Sprint 2) — OPTIONAL.
#
# The HTML report needs only Jinja2 (already in requirements.txt). WeasyPrint is
# required only for the PDF route (POST /cases/report.pdf) and additionally needs
# the pango/cairo system libraries:
#   macOS:  brew install pango
#   Debian: apt-get install libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0
#
# Install with:  pip install -r requirements_report.txt   (or  pip install -e '.[report]')
# CI does NOT install this file; the PDF tests skip gracefully when it is absent.

weasyprint>=60
